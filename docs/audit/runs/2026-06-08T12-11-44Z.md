# Audit run — 2026-06-08T12:11:44Z
**Mode:** TEST_ONE
**Subject:** trainer-invite-student
**Result:** IMPROVED+PENDING+verification

Ran audit spec against prod for the Invite student dialog. All prior fixes
(role=dialog, aria-modal, aria-labelledby, ESC closes, Invite-by-email link
visually distinct, nickname aria-label, mobile dialog fits viewport, no
horizontal overflow, no console/page errors) verified clean.

Found one observable a11y gap: `InviteStudentDialog.tsx` reimplemented modal
plumbing (portal + backdrop + scroll lock + Escape) but never copied the
focus-trap and focus-restore behavior that the shared `Dialog` component has.
The spec logged `FIRST TAB FOCUS: A` after closing — focus had fallen through
to a stray anchor on the page instead of returning to the "Invite student"
trigger. Tabbing inside the open dialog could also escape to elements behind
the backdrop.

Mirrored the focus-trap + previous-focus-restore effect from
`src/components/ui/Dialog.tsx` into `InviteStudentDialog.tsx`. Added `useRef`
on the dialog panel and a `keydown` Tab handler that wraps focus between the
first and last focusable inside the panel; the effect's cleanup restores
focus to whatever was active before the dialog opened. Typecheck + 48
unit/integration tests pass.
