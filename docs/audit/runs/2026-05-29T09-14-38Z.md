# Audit run — 2026-05-29T09:14:38Z
**Mode:** TEST_ONE
**Subject:** trainer-new-opening-study-dialog
**Result:** IMPROVED+PENDING+PR

Exercised the "New opening study" dialog on prod. Auth, dialog open, name input,
side picker (White/Black), prefix board, and Create button validation all behave
correctly. No app console errors, no page errors. At 375x812 the dialog binds to
the viewport (345x780) and side options remain comfortable touch targets
(301x70).

Observation: the SideOption tiles (White / Black) had no `:hover` style — the
inline styles only animated background/border on `picked` state changes, so
moving the mouse over an unselected option gave no visual feedback. The same
selectable-tile pattern in `SettingsPage` uses `className="role-pick"`, which
adds an inset accent ring on hover (and is consistent with the global
`:focus-visible` ring). Adding the class to SideOption brings the dialog in line
with that convention without changing the picked/unpicked look.
