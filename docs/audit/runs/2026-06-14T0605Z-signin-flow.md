# Audit run — 2026-06-14T06:05:46Z
**Mode:** TEST_ONE
**Subject:** signin-flow
**Result:** OK+MERGED

Exercised the sign-in flow at https://praxis.tsrun.dev/ against prod with the bot credentials. The form rendered with email/password inputs and a yellow "Sign in →" submit button. Negative path with empty fields kept the user on `/` via native validation; negative path with a wrong password surfaced a polite `[role="alert"]` reading "Invalid email or password." The submit button showed a visible focus ring (2px solid `rgb(251, 191, 36)` with a matching box-shadow) and a darker hover background (`rgb(245, 181, 16)`). The transient "Signing in" busy label was observable during submit. With valid credentials the page navigated to `/trainer/studies` and rendered the "Studies" heading. No application console errors and no uncaught page errors were observed.

At the 375×812 mobile viewport, `document.documentElement.scrollWidth` was 360 against a client width of 375, so there was no horizontal overflow. Email, password, and submit each measured 44px tall — comfortably above the WCAG 2.5.5 touch-target floor. Marketing content sits above the sign-in form on mobile (form bounding box at y≈2155), which is the existing landing-page layout decision and not a regression. No improvements warranted this rotation.
