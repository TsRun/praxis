# Audit run — 2026-06-14T09:05:17Z
**Mode:** TEST_ONE
**Subject:** trainer-games-browser
**Result:** IMPROVED+PENDING

Exercised /trainer/games against prod with the bot credentials. The "Browse games" heading rendered alongside the source segmented control (Chess.com / Lichess / Database, `role=group`, `aria-label="Game source"`), the Filters card with all its inputs, the position checkbox group, and the empty-state "Run a search to see games." panel. All form inputs (Chess.com username, Year from/to, ECO/opening, Min Elo, Player name) had proper `<label for="">` associations — `labelsFor: 1` for each. Color radios shared a single `name` so arrow-key navigation between them works as a radio group. The Search button correctly disabled itself with title="Enter a Chess.com username to search" when the username field was empty. Focus indicators on text inputs were a visible 2px solid amber outline (`rgb(251, 191, 36)`). No application console errors, no uncaught page errors.

At 375×812 there was no horizontal overflow (scrollWidth 360 vs clientWidth 375) and the import grid stacked vertically as expected. One UI defect spotted: the Year from / Year to inputs are functionally `disabled` whenever the source is Chess.com or Lichess (years are database-only), but the input row carries no visual treatment to communicate that — same opacity, same border, same placeholder color as the enabled inputs. Compare with the Time control row in the same card, where each label already fades to `opacity: 0.5` when disabled (line 793 of ImportGamesPage.tsx). Fix: apply `opacity: yearEnabled ? 1 : 0.5` to the year-row container so the disabled state is visually consistent with the rest of the filter card. One-line change; typecheck + 48/48 unit tests + audit spec all green on the branch.
