# Audit run — 2026-05-30T12:16:35Z
**Mode:** TEST_ONE
**Subject:** student-dashboard
**Result:** IMPROVED+PENDING

Picked oldest-tested live feature `student-dashboard` (lastTestedAt 2026-05-27T21:14:43Z, predating last discovery 2026-05-28T06:23:55Z). Rewrote the audit spec — the previous version was timing out at the desktop screenshot and was conflating the `StudentLayout`'s coach/student role-switcher segmented with the assignments filter. New spec scopes the `.segmented` to the `<h2>All assignments</h2>` row, raises the per-test timeout to 60s, and asserts mobile-overflow alongside H1/H2/MiniStat/ProgressBar a11y. Spec passes against prod in 5.5s with no `PAGE ERRORS` and no application console errors.

Observation that drove the fix: `AssignmentRowCard` declared `transition: 'background 120ms ease, border-color 120ms ease'` inline alongside `cursor: pointer`, but no `:hover` rule ever changed those properties — the assignment rows on the dashboard had a click cursor with zero visual affordance on hover. The wrapping `<Link>` rendered as an inline `<a>` so the global `:focus-visible` outline drew inside the card edge rather than around it.

Fix (`src/index.css` + `src/student/DashboardPage.tsx`, ~15 LOC): added `.assignment-row-link` (display: block, focus-visible outline-offset 3px, anchor reset) and `.assignment-row` (transition + `:hover` background and border-color bumps) classes, then applied them on the link/card in DashboardPage. Removed the now-redundant inline `transition`, `textDecoration: 'none'`, `color: 'inherit'`. Zero behavioural change beyond keyboard/mouse affordance.

Spec adds guarded probes for the new hover and focus-visible behaviour: `if (await page.locator('.assignment-row').count())` then assert background or border colour mutates on `.hover()`, and that the link wrapper computes `display: block`. The guards make the spec green against the current (pre-deploy) prod and start asserting once the change ships.

Typecheck + 37 unit tests green.
