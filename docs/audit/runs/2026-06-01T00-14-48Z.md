# Audit run — 2026-06-01T00:14:48Z
**Mode:** TEST_ONE
**Subject:** trainer-students-filters
**Result:** IMPROVED+PENDING+PR

Re-tested the All / Linked / Invited segmented filter on `/trainer/students`. The
spec passed against prod (`1 passed (7.3s)`) — switching tabs flips `aria-pressed`
and `.active` correctly, the Invited tab shows the filter-aware empty state
("No invited students"), no console or page errors, mobile 375x812 layout wraps
cleanly with no horizontal overflow.

UI quality miss: the segmented filter buttons have **no `:focus-visible` style**.
Spec captured `outlineStyle: 'none'` and only a faint 1px inset `box-shadow` from
the `.active` rule — so a keyboard user tabbing through `All / Linked / Invited`
gets no visible focus indicator (WCAG 2.4.7 fail). Other components in
`src/index.css` already use the project's standard `outline: 2px solid
var(--accent); outline-offset: 2px;` ring (see `.role-pick:focus-visible`,
line 177); the segmented control was just missing the matching rule.

Fix: add `.segmented button:focus-visible, .segmented a:focus-visible` in
`src/index.css` mirroring the existing accent-outline pattern. Three-line CSS
change, no behavior change, applies to every `Segmented` consumer (students
filters, plus any other segmented controls — e.g. opening study viewer tabs).
