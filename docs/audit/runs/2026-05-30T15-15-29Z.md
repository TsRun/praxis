# Audit run — 2026-05-30T15:15:29Z
**Mode:** TEST_ONE
**Subject:** tour-page
**Result:** IMPROVED+PENDING (PR opened)

Picked oldest feature `tour-page` (last tested 2026-05-28T00:19:43Z). Ran audit spec against prod at 1280×800 and 375×812; no page errors, no app console errors. Scene tabs, ProgressBar (`aria-label="Tour progress"`), Pause/Play toggle, and disabled Back at scene 1 all read out correctly. Mobile body fits (`scroll=360, client=375`); scene-tabs row scrolls horizontally as expected.

Observed UI quality issue: the `<Link to="/">Skip</Link>` in the tour TopBar has zero padding, so its tap-target measures **27.6 × 18.8 px** on 375-wide mobile (and ~26 × 19 on desktop). WCAG 2.5.8 Target Size Minimum (AA in WCAG 2.2) requires 24 × 24 px. Since "Skip" is the only top-of-page exit from the tour, an undersized hit-area is the worst place to take the loss.

Smallest fix: add `padding: 10px 12px; margin: -10px -12px` to the `Skip` Link in `src/marketing/TourPage.tsx:131`. The negative margin keeps the visible text exactly where it was; the new tap area is ~52 × 39 px. One-block style change, no layout knock-on. Typecheck and `npm test` (37/37) green.
