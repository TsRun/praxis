# Audit run — 2026-05-28T00:19:43Z
**Mode:** TEST_ONE
**Subject:** tour-page
**Result:** IMPROVED+PENDING (PR opened)

Picked oldest feature `tour-page` (last tested 2026-05-24T20:07:46Z). Ran the audit spec against prod at viewports 1280×800 and 375×812; no page errors, no app console errors, mobile body fits (`scrollWidth=360` vs `clientWidth=375`).

Observed UI quality issue: the global tour-progress bar renders with `role="progressbar"` and full `aria-valuenow/min/max + aria-valuetext`, but `aria-label` is `null`. Screen-reader users hear the percentage but get no name for what the bar measures. Other tour-page a11y looked solid — `h1` sr-only "Praxis product tour", scene-tab `<button role="group" aria-label="Tour scenes">` with `aria-pressed` and per-scene `aria-label`, pause/play toggle with `aria-pressed` + `aria-label`, focus-visible amber outline on tabs, and the disabled `Back` at scene 1 is `<button disabled>`.

Smallest fix: pass `ariaLabel="Tour progress"` to the `ProgressBar` in `src/marketing/TourPage.tsx:83`. The atom already accepts the prop. One-line diff. Typecheck + unit tests green.
