# Audit run — 2026-06-19T09:09:31Z
**Mode:** TEST_ONE
**Subject:** tournaments-view-toggle
**Result:** IMPROVED+PENDING+verification

Audited the Liste/Carte segmented control on `/tournaments`. The toggle mechanics are healthy: clicking swaps `aria-pressed`, the map's lazy chunk loads and renders SVG regions, swapping back to Liste works, no console/page errors, no horizontal overflow at 375x812, and tap targets meet 36px at both desktop and mobile.

The a11y micro-check flagged a real WCAG-AA contrast failure on the pressed state. `.seg-toggle-btn[aria-pressed="true"]` and `.pill-btn[aria-pressed="true"]` both use `background: #5b8cff` with `color: #fff`, which measures **3.16:1** against white text — below the 4.5:1 normal-text threshold. Both selectors share the same value in `src/index.css` and sit in the same toolbar, so darkening them together to `#2563eb` (~5.17:1) is the smallest change that fixes contrast without breaking visual consistency between the cadence chips and the view toggle.
