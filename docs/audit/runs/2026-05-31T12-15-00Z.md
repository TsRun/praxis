# Audit run — 2026-05-31T12:15:00Z
**Mode:** TEST_ONE
**Subject:** trainer-games-free-editor
**Result:** IMPROVED+PENDING+VERIFICATION

Exercised the Free editor mode on /trainer/games (gated by the "Find games passing through this position" checkbox). Palette renders six white + six black piece buttons, an Erase button, and Starting position / Empty board reset controls. Side-to-move radios work; Empty board → FEN `8/8/8/8/8/8/8/8`; Starting position → standard FEN. No page or console errors; desktop overflow 1265/1280, mobile overflow 360/375 — neither clips.

UI finding: the palette piece buttons are toggle buttons (one selected at a time) but expose no `aria-pressed`, so screen-reader users get no audible cue for which piece is the current brush — the active state was conveyed only by a subtle background/border tint. Labels also used single-letter type codes ("White q", "Black p") that aren't friendly when read aloud. Fix in `src/components/board/PositionSetupBoard.tsx`: added `aria-pressed={active}` on every `PaletteButton` and replaced single-letter labels with full piece names (king/queen/rook/bishop/knight/pawn). Same component is used by the tactic puzzle editor so the improvement reaches both surfaces. Test assertions kept tolerant to both old and new labels so the spec passes against pre- and post-deploy prod.
