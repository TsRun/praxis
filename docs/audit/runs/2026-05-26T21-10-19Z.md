# Audit run — 2026-05-26T21:10:19Z
**Mode:** TEST_ONE
**Subject:** profile-menu
**Result:** IMPROVED+PENDING

Picked `profile-menu` (lastTestedAt was null — never tested). The previous attempt PR #107 was closed by the owner with the note that the bot should re-discover and re-open the fix on its next rotation, because the original PR hit a merge conflict on `features.json`. This run re-derives and re-lands the same fix on a fresh branch off current `main` (`4e7f2fa`).

Wrote `tests/audit/profile-menu.spec.ts` and ran against prod. The spec opens the user dropdown from `/trainer/studies` and probes the WAI-ARIA menu-button contract. The unmodified prod build shows:

```
TRIGGER info (collapsed): {
  ariaHaspopup: 'menu', ariaExpanded: 'false',
  ariaLabel: null, ariaControls: null,
}
MENU info (open): { ariaLabel: null, ariaLabelledby: null, ... }
CLOSES ON ESC: false (menu count: 1 aria-expanded: true)
```

Two real a11y bugs:
- **Escape does not close the menu** — keyboard-only users can open the dropdown but have no way to dismiss it without clicking outside.
- **No `aria-controls` on the trigger and no `id`/`aria-label` on the popup** — assistive tech cannot associate the trigger with its menu; the popup announces with no name.

Everything else the spec exercised was sound on prod and is unchanged:
- Trigger `:focus-visible` outline: solid 2 px amber (`rgb(251, 191, 36)`) with 2 px offset. ✓
- `aria-haspopup="menu"` and `aria-expanded` toggle correctly on open/close. ✓
- Outside click closes the menu. ✓
- Menu content shows name, email, role chips, settings link, Quick roles button, Sign out button. ✓
- Quick roles checkboxes each have a wrapping `<label>` so the role names are their accessible names. ✓
- Mobile 375×812: trigger fits inside the 360-px-wide page-head (`x: 181, right: 320`), menu opens at `left: 20, right: 320` (no horizontal overflow), no document overflow (`scrollWidth 360 ≤ 375`). ✓
- Page errors `[]`, app console errors `[]`. ✓

Fix in `src/auth/UserMenu.tsx`:
- `useEffect` while open adds a document `keydown` listener that closes the menu (and exits the inline RolesEditor sub-state) on `Escape`, then returns focus to the trigger via a new `triggerRef`.
- Trigger button gets `aria-controls="user-menu"` and `aria-label="Open profile menu"`.
- Popup gets `id="user-menu"` and `aria-label="Profile menu"`.

No layout, copy, or visual changes. Diff is small (one component file + spec + features.json + this log).

Verification on PR branch:
- `npm run typecheck` ✓
- `npm test` ✓ (37/37)
- `npx playwright test --config=playwright.audit.config.ts tests/audit/profile-menu.spec.ts` against prod ✓ (1 passed — the spec captures the bug as a finding without asserting fixed behavior, so it passes on the unmodified prod build and will continue to pass after deploy).
