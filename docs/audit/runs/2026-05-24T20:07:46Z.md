# Audit run — 2026-05-24T20:07:46Z
**Mode:** TEST_ONE
**Subject:** tour-page
**Result:** IMPROVED

Open-PR census on main: four `bot-audit` PRs in flight (#75 trainer-studies-list, #78 landing-page-render, #81 settings-page, #86 student-dashboard); all four already carry an approve-equivalent COMMENT review from the bot, so REVIEW mode had nothing to do (queue not full — 4/5).

Per the strict rotation rule the next pick would have been `signin-flow` (oldest non-null `lastTestedAt` after the four skipped IDs), but both `signin-flow` and `signup-form-render` live in `src/auth/SignInUpForm.tsx` which is heavily modified by the still-open PR #78, and both already have queued OK-bookkeeping PRs (#84, #85) sitting on main. Opening another improvement against that file or another OK-bookkeeping PR against either feature would be a literal duplicate of work already queued. Skipped both with that justification logged here, and moved to the next clean target: `tour-page` (last touched 2026-05-23T19:19:13Z, lives in `src/marketing/TourPage.tsx` which no open PR is currently touching).

Rewrote `tests/audit/tour-page.spec.ts` to dump document structure, scene-tabs container/button a11y, progress-bar a11y, pause-play toggle state, back/next disabled state, skip-link contrast, first-tab focus-visible, mobile 375 overflow + tabs-row + CTA visibility. Ran against prod: `PAGE ERRORS: []`, `APP CONSOLE ERRORS: []`. The spec surfaced four real UI/a11y findings: (1) the page has no `<h1>` (`h1Count: 0`), only scene-level `<h2>`s; (2) the scene-tabs container (`.scroll-row`) reports `role: null, ariaLabel: null` and all six tab buttons report `ariaPressed: null, ariaCurrent: null, ariaLabel: null` with run-on `textContent` ("1Author", "2Branch", …); (3) the Pause/Play `<Btn>` in the ControlBar reports `ariaPressed: null, ariaLabel: null` so the toggle state isn't exposed; (4) the `ProgressBar` atom rendered at the top of the tour exposes no `role="progressbar"` / `aria-value*` / `aria-label` — but this one is already being fixed globally by the in-flight PR #86 (`student-dashboard`) which adds those attributes + an optional `ariaLabel` prop to `src/components/ui/atoms.tsx`, so deliberately left untouched in this PR to avoid a merge conflict; will be picked up by a follow-up rotation once #86 ships.

IMPROVE on branch `claude/improve-tour-page-2026052420`: added `<h1 className="sr-only">Praxis product tour</h1>` near the top of the page wrapper (plus a standard `.sr-only` utility in `src/index.css`); SceneTabs container gets `role="group"` + `aria-label="Tour scenes"`; each scene-tab button gets `aria-pressed={active}` + `aria-label={`Scene ${i + 1}: ${s.label}`}` so `"1Author"` becomes `"Scene 1: Author"` in the a11y tree; Pause/Play `<Btn>` gets `aria-pressed={paused}` + `aria-label={paused ? 'Play tour' : 'Pause tour'}`. `npm run typecheck` clean. `npm test` 37/37. `npm run build` clean. Audit spec re-run against prod: 1 passed in 4.0s, `PAGE ERRORS: []`, `APP CONSOLE ERRORS: []`. Diff is 6 files / 171 additions / 10 deletions; ~26 LOC of production-code change (the rest is the spec rewrite + two new screenshots). Within guardrails (<300 LOC, <8 files, no forbidden paths). Opened as PR #89, labelled `bot-audit` + `automated`, base `main`, not draft.

Transport: this run log lands on main via a separate bookkeeping branch `claude/audit-log-tour-page-2026052420` because direct push to main is 403-blocked from the sandbox (same pattern as PR #70 / #73 / #76 / #79 / #82 / #84 / #85 / #87). `features.json`'s `tour-page` row is bumped on the improvement PR itself (`lastTestedAt=2026-05-24T20:07:46Z, lastResult="improved", lastPr=null` — the PR URL belongs in `lastPr` once #89 merges, left null until then per past pattern).
