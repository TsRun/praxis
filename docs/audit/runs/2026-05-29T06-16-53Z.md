# Audit run — 2026-05-29T06:16:53Z
**Mode:** TEST_ONE
**Subject:** trainer-new-game-study-dialog
**Result:** OK+MERGED

Opened the New game study dialog from /trainer/studies. Title `New game
study` reads via the role-dialog name; the Close button has
`aria-label="Close"`; the Study name input and PGN textarea both sit
inside `<label>` wrappers with visible text labels ("Study name", "PGN").
The Import game button is correctly disabled while the form is empty.
No application console errors or uncaught page errors during open/close.

UI states observed: on click-focus, both fields apply
`border-color: var(--accent)` and a 3px `var(--accent-soft)` box-shadow,
plus the global `:focus-visible` 2px amber outline with 2px offset —
focus is clearly visible on a dark background. The dialog has
`maxHeight: calc(100dvh - 32px)` and `overflowY: auto` from the earlier
fix; on the 375x812 mobile pass the dialog box was 345×446px at y≈183,
fully inside the viewport with no clipping.

The PGN textarea uses `rows={10}` with `resize: vertical` and a font-mono
placeholder containing a tiny example PGN — long enough to skim, short
enough to not push the dialog off-screen. The error span uses
`color: var(--danger)`; it lacks an `aria-live` for SR announcement, but
that pattern is shared across every dialog in the codebase, so a single
local change here would only fragment the convention — leaving alone.

No improvement warranted. Marking OK and rotating.
