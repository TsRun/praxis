# Audit run — 2026-05-26T09:13:25Z
**Mode:** TEST_ONE
**Subject:** trainer-import-lichess
**Result:** OK+PENDING

Exercised the "Import from Lichess" entry-point on `/trainer/studies`. Clicking the secondary "Import from Lichess" button opens the `NewOpeningStudyDialog` with `lichessHint=true`, showing the title "Import from Lichess", a hint paragraph ("Name the study and pick the student's side. The next screen will ask for the Lichess PGN."), a `Study name` text input, a White/Black side picker, and a primary "Create + import PGN →" button.

UI lens observations, all green:
- WCAG-AAA text contrast against the dialog surface `rgb(20,20,22)`: title 15.60, hint 7.09, label 15.60.
- `:focus-visible` outline on the side-option buttons renders as a 2 px solid amber ring (`rgb(251, 191, 36)`), clearly visible.
- Side toggle is implemented as a `role="group"` + `aria-labelledby="opening-study-side-label"` with `aria-pressed` toggling — works via mouse click and via Space on a focused button.
- Primary submit is correctly disabled when the name field is empty.
- Escape closes the dialog (handled by `Dialog`).
- Mobile 375×812: page `scrollWidth 360 ≤ 375` (no horizontal page overflow), dialog box width 345 px (left=7.5, right=352.5) — fits cleanly inside the 92 vw cap; side options stack vertically (auto-fit grid `minmax(180px, 1fr)`) at 301 px wide × 70 px tall each.
- `pageerror` `[]`, application `console.error` `[]` (only resource-load 4xx/5xx noise filtered out, which is waived).

Bookkeeping-only PR: bumps `trainer-import-lichess` to `lastTestedAt=2026-05-26T09:13:24Z, lastResult="ok"` in `docs/audit/features.json`.
