# Audit run — 2026-05-30T18:13:00Z
**Mode:** TEST_ONE
**Subject:** signin-flow
**Result:** IMPROVED+PENDING+VERIFICATION

PR #149 (`tour-page`) was already handled this rotation, so REVIEW_AND_MERGE had no work. TEST_ONE picked `signin-flow`, the next oldest non-conflicting feature (`lastTestedAt=2026-05-28T03:13:33Z`). The audit spec passed end-to-end against prod (valid sign-in lands on `/trainer/studies`, busy label appears, 0 page errors, 0 app console errors, mobile viewport at 375 has no horizontal overflow). The single UI nit observed was the lowercased, terse error string `invalid credentials` shown on a wrong-password submit — sub-par next to the polished OAuth error map already present in `SignInUpForm`.

Smallest fix: extended `SignInUpForm.tsx` with a `FRIENDLY_AUTH_ERRORS` map and a `friendlyAuthError()` helper that translates the bare backend string to `Invalid email or password.` (sentence case, full punctuation). Unknown messages pass through untouched, so unrelated errors (rate-limit, account-locked, etc.) are unaffected. Diff is 9 LOC in a single frontend file — no backend, auth, or billing paths touched. Typecheck and `npm test` (37/37) both green.
