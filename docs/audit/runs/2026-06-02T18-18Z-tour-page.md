# Audit run — 2026-06-02T18:18:14Z
**Mode:** TEST_ONE
**Subject:** tour-page
**Result:** IMPROVED+PENDING

Re-exercised `/tour` at 1280×800 and 375×812 on https://praxis.tsrun.dev/. h1 `Praxis product tour`, first-scene h2 `Build move by move.`, scene-tab `role="group"` + per-button `aria-pressed`/`aria-label`, progressbar with `aria-valuenow`/`min`/`max`/`aria-label`, Pause/Play `aria-pressed` toggle, Back/Next native `disabled` state, Skip link, and focus-visible outline (`2px solid rgb(251,191,36)`) on the first scene tab all check out. No page errors, no application console errors, no mobile horizontal overflow (`scroll 360 ≤ client 375`).

One UI quality regression observed at 375×812: the scene-tab strip is horizontally scrollable (`scrollWidth 571` vs `clientWidth 360`) but `SceneTabs` does not scroll the active tab into view as the tour auto-advances. Sampled the page over a full 90s reel — `rowScrollLeft` stayed at `0` the entire time, and from scene 4 (`Transposition`, idx=3) onward the active button is fully off-screen (`activeFullyVisible: false` for scenes 4, 5, 6). Half the tour plays with no scene indicator on mobile. Smallest-change fix: in `SceneTabs`, add a `useRef` + `useEffect([idx])` that computes `targetLeft = active.offsetLeft - (row.clientWidth - active.offsetWidth) / 2` and calls `row.scrollTo({ left: Math.max(0, targetLeft), behavior: 'smooth' })`. Only scrolls the row (not the page), no-op at idx=0 (target clamps to 0), and supports future scene reorders since it reads `aria-pressed="true"` instead of hard-coding `idx`. Typecheck + 48 unit tests green on the PR branch; the audit spec still passes against the stale prod build.
