# Audit run — 2026-06-06T00:10:00Z
**Mode:** TEST_ONE
**Subject:** signup-form-render
**Result:** IMPROVED+PENDING+merge_after_verification

Re-ran the spec against prod: `Create account` mode renders correctly — Trainer button defaults to `aria-pressed="true"` (`rgba(251,191,36,0.12)` background, `rgba(251,191,36,0.35)` accent-ring border), Student and Solo default to `aria-pressed="false"`, and clicking Solo toggles the pressed state back and forth. Email/name/password inputs each have a programmatically associated `<label for>` and the right autocomplete values (`email`, `nickname`, `new-password`). Empty-submit triggers native HTML5 `valueMissing` validation on all three required inputs. Submit button at 424×44 with amber accent + dark text, focus outline on email input is unmistakable (2px solid amber + 3px halo). No page errors, no application console errors, no horizontal overflow at 375 (`scroll=360 client=375`), role tiles render at 280×96 each on mobile.

UI gap observed: hovering the unselected role buttons (Student / Solo) gives **no visible affordance** — `getComputedStyle(...).boxShadow` is `'none'` on hover (the cursor changes to pointer but nothing else moves). The `.role-pick` CSS class is already defined in `src/index.css` (lines 191-198) with a `:hover { box-shadow: inset 0 0 0 1px var(--accent-ring) }` rule plus `:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px }`, clearly intended for these exact buttons — but the buttons in `src/auth/SignInUpForm.tsx` never apply `className="role-pick"`. Smallest fix: add the class to the three role-picker buttons. The visual default is unchanged; hover and keyboard-focus gain the intended ring. Spec passes against prod on the PR branch (assertion-stable; new behavior recorded as warnings until the fix is deployed).
