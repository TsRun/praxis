# Audit run — 2026-06-10T00:10:00Z
**Mode:** TEST_ONE
**Subject:** tour-page
**Result:** IMPROVED+PENDING+verification-after-PR-open

Tour page at /tour rendered cleanly on desktop (1280x800) and mobile (375x812): no page errors, no app console errors, headings present, scene tabs labelled (aria-pressed/aria-label), progress bar role/values correct, focus-visible outline (2px solid var(--accent)), and no horizontal overflow at 375 (scrollWidth 360 < 375). Future scene-tab contrast 7.62 against page bg — well above WCAG AA. The audit added a "nested interactives" probe (a>button or button>a) and found one match: the control-bar "Sign up →" CTA is rendered as `<a href="/#auth"><button class="btn btn-primary">Sign up →</button></a>` — invalid HTML (interactive content nested inside an anchor) and an a11y smell (screen readers may announce both, and click handling can swallow link navigation). The same pattern exists in the end-state CtaScene for "Create a free account →" and "Back to home", but those weren't probed because the tour starts at scene 1.

Fix: convert all three call sites in src/marketing/TourPage.tsx from `<a|Link><Btn>...</Btn></a|Link>` to `<a|Link className="btn btn-primary|btn-ghost btn-lg">...</a|Link>`. The existing CSS already styles `.btn`/`.btn-*` purely by class (no `button.btn` selector), and the global `a { color: inherit; text-decoration: none; }` rule keeps the visual identical. Typecheck + 48 unit tests pass.
