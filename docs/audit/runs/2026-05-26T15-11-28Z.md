# Audit run — 2026-05-26T15:11:28Z
**Mode:** TEST_ONE
**Subject:** trainer-new-game-study-dialog
**Result:** OK+PENDING

Exercised the **New game study** dialog on `/trainer/studies` (open the **New study** menu, click **Game study**). Played through the dialog at desktop 1280×800 and mobile 375×812 against the deployed prod build (`76a6e6a`).

UI lens observations, all green:
- **Labels**: both the `Study name` input and the `PGN` textarea are wrapped in a parent `<label>` (`wrappedInLabel: true`) — assistive tech reads "Study name" / "PGN" when focus lands on either control, no `<label for>` plumbing needed.
- **Focus indicator**: programmatic `.focus()` captures the start of the 120 ms transition, but `getComputedStyle()` after the transition settles shows both controls hit the same amber `:focus` state — `border-color: rgb(251, 191, 36)` + `box-shadow: rgba(251, 191, 36, 0.12) 0 0 0 3px` from `.input:focus` / `.textarea:focus`, plus the global `:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px }` ring. Visible focus for both keyboard and mouse activation.
- **Close button**: `aria-label="Close"` is present on the icon button (no visible label, so the aria-label is doing real work).
- **Form validation**: `Import game` submit is correctly disabled when either field is empty (`isDisabled() === true` with both fields blank). Trying to submit blank values via the form would also set `Give the study a name.` / `Paste a PGN.` inline error copy.
- **Mobile 375×812**: dialog bounding box `x=7.5, width=345` — fits cleanly inside the viewport (10 px gutter each side from the `Dialog` component's `max-width: 92vw`). No horizontal page overflow.
- **Page errors**: `[]`. **App console errors**: `[]` (only resource-load 4xx/5xx noise filtered out, which is waived per spec).

Bookkeeping-only PR: bumps `trainer-new-game-study-dialog` to `lastTestedAt=2026-05-26T15:11:28Z, lastResult="ok"` in `docs/audit/features.json`.
