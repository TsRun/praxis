# Audit run: 2026-05-23T17:07:59Z

**Mode:** TEST_ONE  
**Feature:** `signup-form-render` (Sign-up form render, route `/`, requiresAuth: false)  
**Result:** OK

The Playwright spec navigated to `https://praxis.tsrun.dev/`, clicked the "Create account" tab button to switch `SignInUpForm` into sign-up mode, then asserted all expected elements were visible: email input (`placeholder="you@studio.club"`), nickname input (`placeholder="e.g. tactical_torre"`), password input (`type="password"`, `placeholder="at least 8 characters"`), and the three role-picker buttons (Trainer, Student, Solo). The "Create account →" submit button was also confirmed visible. A screenshot was captured to `tests/audit/screenshots/signup-form-render.png`.

No actionable console errors or uncaught page exceptions were observed. The only console noise was the expected browser-generated `401` message from `GET /api/auth/me` on anonymous page load, which is correct HTTP semantics and already handled silently by `AuthContext.tsx`. `features.json` bumped (`lastTestedAt`, `lastResult: ok`) and committed to main via GitHub MCP (git push returns 403 in this sandbox).
