# Audit run — 2026-06-14T03:09:37Z
**Mode:** TEST_ONE
**Subject:** tour-page
**Result:** OK+MERGED

Exercised `/tour` at 1280x800 and 375x812 against prod. The marketing tour
SPA hydrates and renders correctly: `h1` "Praxis product tour", first
scene `h2` "Build move by move.", chessboard column, mainline preview,
progress bar with `role=progressbar` and `aria-valuenow/min/max/label`
("Tour progress"). No page errors, no application console errors.

A11y observations: scene-tab strip is a `role="group" aria-label="Tour scenes"`
container of buttons; the active tab carries `aria-pressed=true` and a
descriptive `aria-label` ("Scene N: <name>"). **Pause/Play** button has
`aria-label="Pause tour"`. **Back** is properly disabled on scene 1, **Next**
enabled. **Skip** link renders at 13px in `rgb(160,160,168)` on the dark
background. Focus-visible on a scene tab paints a solid 2px amber outline
— the global `:focus-visible` ring is intact. No nested interactive
elements (no `a > button` or `button > a`).

Future-scene tab contrast measured 7.62:1 at 12.5px — comfortably above
WCAG AA (4.5) and meets AAA (7.0). Mobile at 375x812: page
`scrollWidth=360` (no horizontal overflow); the scene-tab row uses
`overflow-x: auto` with `scrollWidth=571` so the row scrolls smoothly
(visible partial "Transpo…" in the screenshot confirms the affordance).
Sign-up CTA visible at the bottom of the mobile viewport. Nothing
warrants a code change this rotation.
