# Audit run — 2026-05-30T03:13:30Z
**Mode:** TEST_ONE
**Subject:** landing-page-render
**Result:** IMPROVED+PENDING

Exercised `/` against prod. Functional state is clean: 200 OK, no page errors, no app console errors, no mobile horizontal overflow at 375x812, focus ring on email input renders a 2px solid amber outline, form labels are programmatically associated via `label[for]`, role-picker buttons are correctly tagged with `aria-pressed` + `aria-label`.

The landing footer (`Praxis · made for chess coaches.`) renders at 12.5px with `color: var(--text-faint)` = `#6b6b73` on page bg `#0a0a0c`. That is **~3.75:1**, which fails WCAG-AA for normal text (4.5:1 required, since 12.5px is well below the 18px / 14pt-bold "large text" threshold). Hero paragraph (`var(--text-dim)` = `#a0a0a8`) measures ~7.7:1 — already fine. Smallest fix: switch the landing footer color from `--text-faint` to `--text-dim`, lifting it to ~7.7:1. One-line inline-style change in `src/auth/LandingPage.tsx`. Did not touch the global `--text-faint` token because other surfaces rely on its current value at larger sizes.
