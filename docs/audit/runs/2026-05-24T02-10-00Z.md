# Audit run — 2026-05-24T02:10:00Z
**Mode:** TEST_ONE
**Subject:** trainer-new-opening-study-dialog
**Result:** IMPROVED

Picked `trainer-new-opening-study-dialog` as the oldest never-tested feature whose id isn't already represented by an open `bot-audit` PR. Skipped `trainer-invite-student` and `trainer-new-game-study-dialog` because PRs #61 and #63 are still open against them, and both already have prior reviews by this bot.

Wrote `tests/audit/trainer-new-opening-study-dialog.spec.ts`, logged in via env creds, opened the **New study → Opening study** dialog against prod, and exercised it at 1280 and 375. The dialog itself renders cleanly — `MOBILE DIALOG BOX width: 345` at viewport 375, no overflow; form validation works (`CREATE BTN DISABLED (empty name): true` → `false`). I initially flagged the SideOption focus indicator as missing because `el.focus()` showed `outlineStyle: 'none'`, but re-tested with actual keyboard `Tab` and `el.matches(':focus-visible')` returned `true` with a `2px solid rgb(251, 191, 36)` outline from the global `:focus-visible` rule — so focus is fine. The Dialog close button still has no `aria-label`, but that lives in `src/components/ui/Dialog.tsx` and PR #63 is already fixing it — out of scope to avoid the conflict.

The actionable observation: both White/Black SideOption buttons render with `aria-pressed: null`, so screen readers can't tell which side is selected, and the picker container has no group label associated with the visible "Which side does the student play?" question. Filed PR #64 against `claude/improve-trainer-new-opening-study-dialog-2026052402` with a minimal additive fix in `src/trainer/NewOpeningStudyDialog.tsx`: `aria-pressed={picked}` on each SideOption, and `role="group"` + `aria-labelledby` on the container pointing to a new id on the question span. `npm run typecheck` clean; `npm test` 37/37; audit spec 1 passed in 6.3s; `PAGE ERRORS: []`, `APP CONSOLE ERRORS: []`. CI on PR #64 reports `total_count: 0` (no checks configured on this repo); no review comments yet — nothing actionable to address on the watch subscription.
