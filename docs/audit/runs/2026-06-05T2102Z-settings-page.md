# Audit run — 2026-06-05T21:02:27Z
**Mode:** TEST_ONE
**Subject:** settings-page
**Result:** OK+MERGED

Re-tested `/settings` against prod with the bot account (audit Playwright spec, 1/1 passed in 5.7s, default 1280×800 viewport then 375×812 mobile pass). All four cards rendered cleanly — Profile, Roles, Password, API keys — with no uncaught page errors and an empty application console-error list after filtering the standard `Failed to load resource` waiver. The bot has 0 active API keys, so the Revoke icon-button row was not exercised in the live tree this rotation; the New-key affordance (`<button>+ New key</button>`, height 28px) and the empty-state copy "No keys yet. Mint one to plug into the Praxis MCP server." rendered as expected.

A11y micro-check is solid. Profile inputs are wrapped in `<label>` elements with the visible label text ("Nickname", "Email") serving as the accessible name, and the inputs carry the right `autocomplete` values (`nickname`, `email`). Password inputs do the same with `current-password` / `new-password` / `new-password`. The Roles card exposes `role="group"` `aria-label="Active roles"` over a 3-column CSS grid; each tile is a `<button type="button">` with `aria-pressed="true"|"false"` and `aria-label="Trainer — I coach others"` (and the two siblings). Focus state on the Nickname input is unmistakable: `outline-style: solid`, `outline-width: 2px`, `outline-color: rgb(251,191,36)` plus a 3px amber-soft box-shadow halo — well above the WCAG 2.4.7 minimum. Tab order from Nickname lands on the Email input (`autocomplete="email"`). The Save changes button reports `disabled=true` while the form is not dirty, which is the correct interaction model.

Mobile 375×812 pass: `document.documentElement.scrollWidth` is 360 against a 375 client width — no horizontal overflow. The Roles grid keeps its 3-column tile layout with each button rendering at 89px width (`MOBILE ROLE BUTTON WIDTHS: [89, 89, 89]`), still above the >60 threshold; the sub-copy ("A coach assigns me", "I coach others", "My own materials") wraps to 2–3 lines inside each tile but remains legible, and the tiles align to equal height under grid auto-rows. The container max-width (720) collapses to the viewport-minus-padding (`clamp(16px, 4vw, 28px)` outer + 22px card padding), and every card fits without inner scrollers. Two screenshots written to `tests/audit/screenshots/` (desktop + mobile) for the rotation. Nothing to fix on this rotation.
