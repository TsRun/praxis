# Audit run — 2026-05-30T21:09:45Z
**Mode:** TEST_ONE
**Subject:** trainer-games-browser
**Result:** IMPROVED+PENDING+daily-cap

`tests/audit/trainer-games-browser.spec.ts` passed against prod (0 page errors, 0 app console errors). The mobile-viewport probe (375×812) flagged horizontal overflow: `scrollWidth=460` vs `clientWidth=375` — an 85px overflow caused by the side-by-side `Year from` / `Year to` number inputs (each 210px wide) in the Filters card. The `<input>` elements ignored their flex parents because the shared `inputStyle` had no `width` and number inputs default to a min-content width above the 375-divided-by-2 budget.

Smallest fix: add `width: '100%', minWidth: 0, boxSizing: 'border-box'` to the `inputStyle` constant in `src/trainer/ImportGamesPage.tsx`. All inputs styled with `inputStyle` (Chess.com username, Lichess username, Player name, Year from, Year to, ECO, Min Elo) now respect their flex/grid parents. `npm run typecheck` ✅. `npm test` ✅ (37/37). Audit spec still passes on prod.

REVIEW_AND_MERGE check skipped: open bot-audit PRs #149 (tour-page) and #150 (signin-flow) already have my `<!-- audit-verification -->` comments and are rolled over for the daily-cap reset. The new `claude/improve-trainer-games-browser-2026053021` PR is also subject to today's 4/4 `fix(audit):` cap (#143, #144, #145, #147), so it will be opened green with a verification comment and held for the next window.
