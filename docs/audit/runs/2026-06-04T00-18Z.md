# Audit run — 2026-06-04T00:18:00Z
**Mode:** TEST_ONE
**Subject:** trainer-tactic-set-editor
**Result:** IMPROVED+PENDING+PR

Exercised `/trainer/studies/tactic/:id` end-to-end as the trainer bot — created
a fresh tactical set, landed on the editor, and probed UI/a11y at 1280 and
375 viewports. No page errors, no application console errors, no mobile
overflow (scrollWidth 360 at 375 viewport).

The interactive H1 title (`EditableTitle`) exposed click-to-rename via
`cursor: pointer` + `title="Click to rename"`, but had no `tabindex`,
no `role`, and no keyboard handler — keyboard-only users could not focus
or activate the rename. Buttons (`Add puzzle`, `Delete set`, `Assign to
student`, `Studies`) all painted the global `:focus-visible` ring under
real keyboard tabbing, so the only concrete gap was `EditableTitle`.

Fix wraps the display-mode heading text in a real `<button>` inside the
`<h1>` / `<h2>`. The heading stays a heading (preserving document outline
and SR heading navigation); the button gets native Tab/Enter/Space and
the global focus ring. Single-file change in `src/components/ui/EditableTitle.tsx`,
inherited by `TacticSetPage`, `OpeningStudyEditor`, and `GameStudyEditor`.
Typecheck + unit tests pass locally.
