# Audit run — 2026-05-24T19:10:23Z
**Mode:** REVIEW
**Subject:** PR #86 (`student-dashboard`)
**Result:** APPROVED

Open `bot-audit`-labelled PRs at run-start: #75, #78, #81. All three already carry a COMMENT review by this bot (PR #75 reviewed in PR #77 / 2026-05-24T11:13:02Z; PR #78 reviewed in PR #80 / 2026-05-24T13:09:55Z; PR #81 reviewed in PR #83 / 2026-05-24T15:07:17Z). None is un-reviewed, so the strict REVIEW-queue is empty.

PR #86 (`[bot] Improve Student dashboard (progress bar + filter + tile a11y)`, `claude/improve-student-dashboard-2026052418`, created 2026-05-24T18:12:38Z) was un-reviewed but is missing the `bot-audit` label that earlier IMPROVE PRs in the rotation carry — this looks like a labeling oversight at create-time, not an intentional opt-out. The title, body, branch prefix, and diff shape all match the IMPROVE pattern (audit spec under `tests/audit/`, screenshots under `tests/audit/screenshots/`, `features.json` bump, ~25 LOC of production-code a11y). Took the spirit-of-the-spec call and reviewed it.

Checked out `pr-86` (= `claude/improve-student-dashboard-2026052418` at `068570d`):

- `npm run typecheck` → clean.
- `npm test` → 6 files, 37 / 37 pass.
- `npm run build` → `✓ built in 2.80s`.
- `npx playwright test tests/audit/student-dashboard.spec.ts --config=playwright.audit.config.ts --reporter=line` → 1 passed in 7.8s; `PAGE ERRORS: []`, `APP CONSOLE ERRORS: []`.

The audit spec runs against prod (not the PR branch), so its dumps independently reproduce the OBSERVED state on the currently-deployed `main`. The recorded diagnostics on this run match the PR description's claims exactly: `TODAY card: { hasProgress: false, progressRole: undefined, progressAriaValueNow: undefined, progressAriaLabel: undefined, avgText: '0% average' }`, `SEGMENTED first: { role: null, ariaLabel: null, ariaLabelledby: null }` (with correct per-button `aria-pressed: 'true' | 'false'`), and `MINI STATS: [{ tileRole: null, tileAriaLabel: null, ... }, { tileRole: null, tileAriaLabel: null, ... }]`. Mobile pass is clean: `MOBILE OVERFLOW: { scroll: 360, client: 375 }`, sections stack cleanly (`No active assignments` 316 → `All assignments` 455 → `Today` 702 → `Activity` 835).

The diff fixes exactly those:

- `src/components/ui/atoms.tsx` (L231-254): `ProgressBar` gains an optional `ariaLabel?: string` prop and the outer `<div>` becomes `role="progressbar" aria-valuemin=0 aria-valuemax=100 aria-valuenow={rounded} aria-valuetext={`${rounded}%`} aria-label={ariaLabel}`. Existing callers in `TourPage.tsx` and `OpeningStudyViewer.tsx` are unaffected — `ariaLabel` is undefined, React drops the attribute, and percent is still announced via `aria-valuetext`.
- `src/student/DashboardPage.tsx`:
  - L280-285: Active/Completed `<Segmented>` gains `ariaLabel="Filter assignments by state"` (Segmented atom already wires that to `role="group"` + `aria-label` on the container).
  - L346-349: Today's `<ProgressBar>` gets `ariaLabel="Today's average progress across assignments"`.
  - L604-610: Per-row assignment `<ProgressBar>` gets `ariaLabel={`${a.name} progress`}` so the bar is announced with the study name it belongs to.
  - L455-489: `MiniStat` tile `<div>` gets `role="group"` + `aria-label={`${value} ${label}`}`; inner value/label `<span>`s gain `aria-hidden="true"` so the pair is announced once as a single named tile instead of two unrelated text nodes.

Purely additive a11y. No visual, copy, or layout change. 6 files / 282 additions / 4 deletions — of which 257 additions are the new audit spec + 2 PNG screenshots, so ~25 LOC of production-code change. Well under the 300 LOC / 8 files limit. No forbidden paths touched.

Approve-equivalent COMMENT review posted on PR #86 (GitHub blocks self-approval — same workaround as PR #75 / #78 / #81 reviews). No code changes on main. No `features.json` mutation (REVIEW runs don't bump feature state). Run-log routing via `claude/audit-log-2026052419` branch (direct push to main is 403-blocked from this sandbox, same workaround as PR #65 / #67 / #68 / #70 / #71 / #73 / #74 / #76 / #77 / #79 / #80 / #82 / #83).
