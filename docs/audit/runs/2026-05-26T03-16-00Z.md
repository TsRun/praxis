# Audit run — 2026-05-26T03:16:00Z
**Mode:** TEST_ONE
**Subject:** student-dashboard-assignments-tabs
**Result:** IMPROVED+PENDING

Exercised the `Active` / `Completed` segmented filter under the "All assignments" card on `/student/dashboard`. Functional behaviour is sound on prod: the group has `role="group"` and `aria-label="Filter assignments by state"`, each button toggles `aria-pressed` and the `.active` class, keyboard `:focus-visible` shows the accent outline ring (2px solid `rgb(251,191,36)` at 2px offset), and Space-key activation works. Click and tap both switch the visible body. No page errors, no app console errors, no horizontal overflow at 375×812 (`scrollWidth 360 ≤ 375`).

UI bug observed on the mobile screenshot (375×812): the "All assignments" `<h2>` wraps onto two lines because the inline `Segmented` control consumes ~160px of the 339px card content area, leaving the heading too narrow. The count chip ("1") sits awkwardly beside the wrapped word. Smallest-change fix in `src/student/DashboardPage.tsx`: add `flexWrap: 'wrap'` + `rowGap: 10` / `columnGap: 12` to the header row, and `whiteSpace: 'nowrap'` to the title-and-count subgroup so it stays on one line; the segmented falls below on narrow widths. Desktop layout is unchanged because the row still fits without wrapping. Typecheck ✓, `npm test` ✓ (37 passed).
