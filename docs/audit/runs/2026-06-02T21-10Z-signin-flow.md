# Audit run — 2026-06-02T21:10:27Z
**Mode:** TEST_ONE
**Subject:** signin-flow
**Result:** OK

Exercised the sign-in form on https://praxis.tsrun.dev/ at the default 1280 viewport and at 375×812. Negative paths (empty submit kept the user on `/` via native validity; wrong password rendered "Invalid email or password." in the polite `role="alert"` region) and the positive path (valid creds → redirect to `/trainer`, heading "Studies" rendered) all behaved correctly. The transient "Signing in…" busy label was caught during submission and the submit button kept its 44px height on mobile. The `:focus` outline on the submit button is a solid 2px amber-400 ring (`rgb(251,191,36)`) with 2px offset — clearly visible. `:hover` shifts the background to `rgb(245,181,16)`, also visible. No page errors, no application console errors at either viewport, no horizontal overflow at 375 (`scroll=360 client=375`).

Minor observation worth a note: the `.input` class is globally 36px tall (vs `.input-lg` 44px used on the submit button), which puts the email + password fields at the WCAG 2.5.5 Level-AAA threshold but still above the Level-AA 24px minimum. No functional regression — tapping each input opens a soft keyboard cleanly on the mobile screenshot. Holding off on a global `.input` height change because that's a cross-app design call, not a sign-in-only fix. Decision: OK, rotate.
