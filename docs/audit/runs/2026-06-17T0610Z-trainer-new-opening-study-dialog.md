# Audit run — 2026-06-17T06:10:25Z
**Mode:** TEST_ONE
**Subject:** trainer-new-opening-study-dialog
**Result:** IMPROVED+PENDING (PR open)

Re-exercised the New opening study dialog from `/trainer/studies` at 1280×800 and 375×812. The June-12 stacking-context fix has held: pieces no longer paint over the footer (`TOPMOST AT CREATE-BTN CENTER (mobile): { className: 'btn btn-primary', insideFooterButton: true }`). A11y stays solid — wrapping `<label>` for Study name, `aria-labelledby` on the radiogroup, amber 2px focus outline on the visible side tile, dialog has `role="dialog"` + `aria-modal="true"` + `aria-labelledby`. No console errors, no page errors.

New defect, visible in **both** screenshots this run: the opaque sticky footer **paints over the move-list hint** at the initial scroll position. `MOBILE DIALOG BOX: { scrollH: 871, clientH: 780 }` — the dialog overflows by 91 px, so at scrollTop=0 the bottom ~64 px of the content area (where `PrefixMoveList` lives) is covered by the sticky footer with `background: var(--card-bg)`. Visually the hint "Standard start — drag a piece to set a prefix." reads as truncated through the footer — confusing on a first-time dialog. The previous fix (`isolation: isolate` on the board) addressed pieces clipping over the footer; it didn't address the footer clipping the move-list. Sticky-with-opaque-bg over a scrollable region inherently covers content at non-max scroll positions.

Fix in `src/trainer/NewOpeningStudyDialog.tsx`: split the form into an inner scroll region + an outer (non-sticky) footer. The form is now `flex: '1 1 auto'; minHeight: 0` so it grows to natural size when the dialog fits and shrinks only when the dialog hits its `maxHeight: calc(100dvh - 32px)`. The first child (everything except the footer) gets `overflowY: auto` + `flex: '1 1 auto'` + `minHeight: 0` — content scrolls inside that region, never *through* the footer. The footer keeps its top hairline + the existing `-22 / +22` inline margin trick so the separator still spans the full dialog width, and the buttons stay visible at the bottom on short viewports because flex-shrinking the form (not the footer) is what absorbs the overflow. Net: −1 LOC of background paint, no logic change; typecheck and all 48 unit tests pass.
