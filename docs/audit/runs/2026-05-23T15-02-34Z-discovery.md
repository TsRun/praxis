# Audit Run — Discovery — 2026-05-23T15:02:34Z

## Mode
DISCOVERY — `docs/audit/features.json` did not exist.

## Setup
- `npm ci` completed successfully.
- Playwright Chromium installed (v1223, headless shell).
- Bot credentials loaded from literal fallback values (env vars not set in sandbox).

## Crawl
Wrote `tests/audit/_discovery.spec.ts` (not committed). Used a custom `playwright.audit.config.ts` pointing at `https://praxis.tsrun.dev` with `ignoreHTTPSErrors: true` (self-signed cert on prod domain).

**Unauthenticated routes:** `/`, `/tour`, `/role-picker` — all returned HTTP 200. The app is an SPA (React Router); all unauth routes serve the landing page HTML shell and redirect unauthenticated visitors to the landing marketing content.

**Login:** The sign-in form (`input[placeholder="you@studio.club"]` + `input[type="password"]`) is rendered directly on the landing page at the `#auth` anchor. Submitted credentials via `page.fill` + `submitBtn.click()`. After 4 s the URL resolved to `https://praxis.tsrun.dev/trainer/studies`, confirming the bot account (`claude.bot@gmail.com`) has a **trainer/self** role. Auth storage state saved to `.auth/bot.json`.

**Authenticated routes crawled:**
- `/trainer/studies` — "Studies | Opening studies | Game studies | Tactical sets"; buttons: Import from Lichess, New study, New game study
- `/trainer/students` — "Students"; buttons: Invite student, All/Linked/Invited filters
- `/trainer/games` — "Browse games"; Chess.com / Lichess / Database / Free editor tabs; board controls (Play moves, Undo, Reset)
- `/student/dashboard` — "Good afternoon, Claude Bot | No active assignments"; Active/Completed filter
- `/settings` — Profile, Roles, Password, API keys sections

## Features inventoried (15)

| id | route | auth? |
|----|-------|-------|
| landing-page-render | / | no |
| signin-flow | / | no |
| signup-form-render | / | no |
| tour-page | /tour | no |
| trainer-studies-list | /trainer/studies | yes |
| trainer-new-study-menu | /trainer/studies | yes |
| trainer-new-opening-study-dialog | /trainer/studies | yes |
| trainer-new-game-study-dialog | /trainer/studies | yes |
| trainer-new-tactic-set-dialog | /trainer/studies | yes |
| trainer-import-lichess | /trainer/studies | yes |
| trainer-students-list | /trainer/students | yes |
| trainer-invite-student | /trainer/students | yes |
| trainer-games-browser | /trainer/games | yes |
| student-dashboard | /student/dashboard | yes |
| settings-page | /settings | yes |

## Result
`docs/audit/features.json` written with 15 features, all `lastTestedAt: null`. Next run will enter MODE=TEST_ONE and begin testing the first feature alphabetically (by id, null timestamps sort first).
