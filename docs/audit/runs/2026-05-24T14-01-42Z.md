# Audit run — 2026-05-24T14:01:42Z
**Mode:** TEST_ONE
**Subject:** settings-page
**Result:** IMPROVED

Picked `settings-page` as the next target: oldest `lastTestedAt` (`2026-05-23T15:26:53Z`) after skipping `trainer-studies-list` (null but has open PR #75). Open `bot-audit` queue at start was 2 (PRs #75 and #78) — both already have prior COMMENT reviews authored by this bot account, so no REVIEW work to do. Mode resolved to TEST_ONE.

Wrote `tests/audit/settings-page.spec.ts` and ran it via `playwright.audit.config.ts` (base URL `https://praxis.tsrun.dev`). The spec signs in via the public form, visits `/settings`, asserts the four section headings (Profile / Roles / Password / API keys), and dumps the actually-rendered a11y state of each interactive element. Captured screenshots at 1280 and at 375×812.

Observed on prod (`PAGE ERRORS: []`, `APP CONSOLE ERRORS: []`):

- **Roles toggle buttons** announced as `"TrainerI coach others"` / `"StudentA coach assigns me"` / `"SoloMy own materials"` — title and sub spans concatenated with no separator. `ariaPressed=null`, `ariaLabel=null`, `role=null` on all three. The `.grid-3` container has `role=null, ariaLabel=null`. Same screen-reader anti-pattern as the landing-page role picker fix that shipped in PR #78.
- **Form error regions** (Profile, Roles, Password, NewKeyDialog) are conditionally mounted `<span>` elements with no `role="alert"` / `aria-live`. When validation fails (e.g. "Pick at least one role", "New password must be at least 8 characters"), the danger text appears but is not announced.
- **Revoke icon button** (only reachable when a key row exists; account had 0 keys at test time, so logged as "no keys rendered") is icon-only with only a generic `title="Revoke key"` and no `aria-label`. It also doesn't name *which* key would be revoked.
- **Inputs** are wrapped in `<label>` via the `Field` helper — implicit association works fine, so no fix needed there. `:focus-visible` already styled (2px amber outline + soft box-shadow). Tab order is sensible.
- **Mobile 375×812** — no page overflow (`scroll: 360`, `client: 375`). `.grid-3` collapses to one column cleanly (button widths 284 each). No layout breakage to fix.

Decided IMPROVE. Edited `src/auth/SettingsPage.tsx`: imported `useId`, added `errId = useId()` to each of ProfileCard / RolesCard / PasswordCard / NewKeyDialog, converted the four conditional error spans to always-rendered `role="alert" aria-live="polite"` regions toggled via `display`. Added `aria-describedby={errId}` to the Password "Change password" and NewKey "Mint key" buttons while an error is showing. On the Roles card, gave the `.grid-3` container `role="group" aria-label="Active roles"`, and each role button `aria-pressed={on}` plus `aria-label={`${title} — ${sub}`}` (so screen readers say "Trainer — I coach others" instead of "TrainerI coach others"); the decorative inner `<Icon>` gets `aria-hidden="true"`. Upgraded the Revoke icon button's `title="Revoke key"` to per-key `title`/`aria-label={`Revoke key "${k.name}"`}` and hid the inner `<IconTrash>` from a11y.

Verified: `npm run typecheck` clean, `npm test` 37/37 pass, `npm run build` `✓ built in 1.95s`, and re-ran the audit spec against prod (1 passed, no page/app console errors). Diff: 3 files / 265 additions / 11 deletions — 195 of those additions are the new audit spec, so ~70 LOC of production-code change. No forbidden paths touched.

Branch `claude/improve-settings-page-2026052414` pushed; opened PR #81 (https://github.com/TsRun/praxis/pull/81) labeled `bot-audit` + `automated`. Bumped `features.json` on the PR branch (`lastTestedAt=2026-05-24T14:01:42Z`, `lastResult="improved"`). Transport: GitHub MCP server (no `gh` CLI in this sandbox); `git push` worked because of the `claude/` branch prefix.

Subscribed to PR #81 activity per request. Initial check: no CI check runs configured for the repo (combined status `pending`, `total_count: 0`), no review threads, no issue comments — nothing actionable yet.
