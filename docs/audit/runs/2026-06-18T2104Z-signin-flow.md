# Audit run — 2026-06-18T21:04:29Z
**Mode:** TEST_ONE
**Subject:** signin-flow
**Result:** OK+MERGED

Re-exercised the sign-in flow on `https://praxis.tsrun.dev/` against bot credentials. From the email field, keyboard tab order is email → password → "Sign in →" submit → page nav (Skip-to-main, wordmark) — no traps, no dead stops. Submit button gains a solid 2px amber (`rgb(251,191,36)`) outline + `rgba(251,191,36,0.28)` 24px box-shadow halo on focus, and shifts to a slightly deeper amber (`rgb(245,182,17)`) on hover; both states are visually obvious against the dark surface. The `div[role="alert"]` error region is present from page-load with `aria-live="polite"` and starts empty, so a wrong submit ("Invalid email or password.") is announced without yanking focus. Empty submit relies on HTML5 required-validation and keeps the user on `/`. The submit button briefly swaps to a "Signing in…" busy label during the request (caught via the rolled-up race on the in-flight click). Successful login lands on `/trainer` and the "Studies" h1 mounts within 15s.

At 375×812, the sign-in card itself is 280px wide centred (40px gutters) with email/password/submit all at exactly 44px tall — i.e. WCAG 2.5.5 compliant tap targets. Page reports `scrollWidth=360 < clientWidth=375` so there is no horizontal overflow; the form sits well below the hero/features fold but is reachable via the in-page "#auth" anchor (Get started / hero CTA / nav Sign in link). Zero application console errors and zero uncaught page errors during the full negative-then-positive run. Nothing actionable surfaced in this rotation — marking OK and bumping `lastTestedAt`.
