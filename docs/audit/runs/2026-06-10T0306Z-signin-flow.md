# Audit run — 2026-06-10T03:06:15Z
**Mode:** TEST_ONE
**Subject:** signin-flow
**Result:** IMPROVED+PENDING

Exercised the sign-in flow on prod at 1280x720 and 375x812. Functionally everything works: empty submit kept on `/`, wrong password surfaced `Invalid email or password.` via the polite `role=alert`, valid creds saw the transient `Signing in…` busy label and landed on `/trainer/studies`. No page errors, no application console errors. Focus and hover styles on the submit button are good (2px amber outline + darkened bg).

UI observation at 375x812: `.input` is 36px tall while the primary submit `Btn size="lg"` is 44px. The size mismatch is visible on the sign-in form (email/pw both 280x36, submit 280x44) and the 36px inputs sit below the widely-cited WCAG 2.5.5 mobile tap-target minimum of 44px. Smallest fix: add `@media (max-width: 640px) { .input { height: 44px; } }` to `src/index.css` so every `.input` across the app meets the mobile tap-target floor and visually matches the lg primary button. One-line CSS change, no JS touched, typecheck + 48 unit tests pass.
