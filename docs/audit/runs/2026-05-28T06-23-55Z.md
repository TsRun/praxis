# Audit run — 2026-05-28T06:23:55Z
**Mode:** DISCOVERY
**Subject:** n/a
**Result:** DISCOVERED (+1 -0 kept 19)

All 19 existing non-removed features have `lastTestedAt > lastDiscoveryAt` (2026-05-25T15:57:46Z), satisfying the cycle-complete trigger. Re-crawled the public routes (`/`, `/tour`, `/role-picker`), logged in as the bot, and walked the trainer studies/students lists and the student dashboard to surface anchor targets, button labels, and nested route IDs. `/role-picker` redirects authenticated users to their default landing and renders the landing form for anonymous visitors, so it does not constitute a distinct testable feature for the bot identity. The trainer-side study editors at `/trainer/studies/opening/:id` etc. exist as routes but the bot trainer has no own studies populating the list, so synthetic IDs are not exercisable without write actions the audit policy forbids.

One genuinely new public-shaped route was confirmed: `/student/study/opening/3` loaded cleanly with no errors as the bot student, exposing the opening-study viewer page (chess-board UI, minimal text — `<body>.innerText` ≈ 54 chars, which is the kind of low-text canvas page the UI-focus rotation should still cover for layout / a11y / responsive). Added `student-opening-study-viewer` to features.json (route `/student/study/opening/:id`, requiresAuth=true) so the next TEST_ONE rotations will start to include it. `lastDiscoveryAt` bumped to 2026-05-28T06:23:55Z, `discoveryCount` → 2, total features → 20.
