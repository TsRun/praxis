# Audit run — 2026-06-05T16:16:07Z
**Mode:** TEST_ONE
**Subject:** trainer-studies-list
**Result:** OK+MERGED

Re-tested `/trainer/studies` against prod with the bot account. Spec passed (1/1, 9.4s). Page rendered `<h1>Studies</h1>`, the three stat tiles (studies authored / students linked / chapters total), and the three labelled sections — Opening studies (3), Game studies (4), Tactical sets (1). No page errors, no application console errors. Sign-in routed to `/trainer/studies` cleanly.

A11y micro-check: the page-level filter `.segmented` (idx 1) exposes `role="group"` with All / Opening / Game / Tactic each carrying `aria-pressed` (`"true"` on All, `"false"` elsewhere). The view-toggle `.segmented` (idx 2) sets `aria-label="View mode"` and its icon-only buttons carry `aria-label="Grid view"` / `"List view"` plus `aria-pressed`. The `#new-study-trigger` button exposes `aria-haspopup="menu"`, `aria-expanded="false"`, `aria-controls="new-study-menu"`. Tab from the search input lands on the **All** filter button with a 2px solid amber outline (`rgb(251, 191, 36)`) — focus ring clearly visible against the dark card background. Section count `.mono` spans render at `rgb(160, 160, 168)` 13px — against the dark app background that ratio is well over 7:1, comfortably passing WCAG-AA. Mobile 375x812 pass: `document.documentElement.scrollWidth` is 360 vs a 375 client width — no horizontal overflow. The page-head Import-from-Lichess + New-study buttons fit in the same row (right edges 188 and 336 against 375). The filter inset row reports width 336 / scrollWidth 334 — no internal overflow. Card hover state is live: a study card transforms from `none` to `translateY(-1px)` and the box-shadow doubles in intensity over a 0.12s transition. Nothing to fix on this rotation.
