# Audit run — 2026-05-24T13:06:24Z
**Mode:** REVIEW
**Subject:** PR #78 — `[bot] Improve Landing page render` (feature `landing-page-render`)
**Result:** APPROVED (posted as COMMENT — GitHub blocks self-approval)

Picked up PR #78 as the oldest open `bot-audit`-labelled PR without a prior review from this bot. The only other open `bot-audit` PR (#75, trainer-studies-list) already carries a verified COMMENT review from a prior rotation, so it was skipped. Total open `bot-audit` PRs: 2 — well under the 5-PR queue-full cutoff.

Fetched `refs/pull/78/head` into a local `pr-78` branch and checked it out. Diff vs main is 6 files / 258 additions / 13 deletions: `src/auth/SignInUpForm.tsx` (~40 LOC of production code: `useId`, `htmlFor`/`id` wiring, `type="email"`, `aria-pressed`/`aria-label`/`role="group"`/`aria-labelledby`/`role="alert"`/`aria-live`, `aria-hidden` on the decorative icon), `src/index.css` (single line `body { overflow-x: clip; }` to contain the hero radial-gradient glow's -10% inset overshoot at 375 wide), `tests/audit/landing-page-render.spec.ts` (215-LOC audit spec, gitignored normally, `git add -f`ed), two screenshots, and a `features.json` bump. Well within the 300 LOC / 8 files / forbidden-paths limits — `src/auth/` is frontend, the off-limits path is `server/auth/`.

Verification on the PR branch: `npm run typecheck` clean, `npm test` 37/37 across 6 files, `npm run build` clean (`✓ built in 2.31s`), and `npx playwright test tests/audit/landing-page-render.spec.ts --config=playwright.audit.config.ts` 1 passed in 6.0s with `PAGE ERRORS: []` and `APP CONSOLE ERRORS: []`. Because the spec targets prod (https://praxis.tsrun.dev/) rather than the PR branch, its diagnostic dumps independently confirm the OBSERVED issues still exist on the currently-deployed main: email input `id=null, labelsFor=0, type="text"`, mode-toggle buttons `ariaPressed=null`, role-picker buttons `ariaPressed=null, ariaLabel=null` with run-on `"TrainerI coach others"` accessible text, and `MOBILE OVERFLOW: { scroll: 382, client: 375 }` (the 7px horizontal scroll at phone width). Every one of those is addressed by the PR's diff; once the PR deploys, a re-run will dump the corrected state.

Decision: APPROVE. Posted as a `COMMENT` review (event=APPROVE on a self-authored PR returns "Can not approve your own pull request" — same workaround used on PRs #66, #69, #72, #75). Posted at https://github.com/TsRun/praxis/pull/78. No code changes from this run, no `features.json` mutation (REVIEW runs don't bump feature state — the IMPROVE PR itself already did `lastTestedAt=2026-05-24T12:11:00Z, lastResult="improved"`). No new branch, no new PR. Cleaned up by checking out main.

Transport: GitHub MCP `pull_request_read` (get_reviews) for prior-review detection on #75 + #78, `pull_request_review_write` (create with `event=COMMENT`) for the review submission.
