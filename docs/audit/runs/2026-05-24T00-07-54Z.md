# Audit run — 2026-05-24T00:07:54Z
**Mode:** TEST_ONE
**Subject:** trainer-new-game-study-dialog
**Result:** IMPROVED

Picked `trainer-new-game-study-dialog` (oldest null `lastTestedAt`, id-ascending after skipping `trainer-invite-student` which has the open PR #61). Wrote `tests/audit/trainer-new-game-study-dialog.spec.ts` and ran it against `https://praxis.tsrun.dev/` with the bot creds. Logged in, opened the **New study → Game study** dialog, exercised the inputs and inspected computed styles on focus.

The spec confirmed two UI/a11y observations. Most impactful: the PGN textarea is inline-styled with `outline: 'none'` and has no replacement `:focus` styling — computed focus style was `outlineStyle: 'none', boxShadow: 'none', borderColor: 'rgba(255,255,255,0.04)'` (unchanged from idle). Compared with the Name input's `.input:focus` (border-color → accent, 3px accent-soft ring), the textarea gave keyboard users zero visible focus indicator — WCAG 2.4.7. Second: the shared `Dialog` component's close (X) `<Btn>` is icon-only with no `aria-label`, so screen readers announce just "button" (`CLOSE BUTTON: { ariaLabel: null, textContent: '' }`). The shared component is used by every "new study" dialog, so the fix lifts a11y for several flows at once. Implicit `<label>`-wrapping was already in place on both inputs, so no label-association change was needed. Form validation disables the **Import game** button correctly when fields are empty; mobile (375x812) fit was clean — dialog 345w with no horizontal overflow.

Fixed both: added `aria-label="Close"` to the shared `Dialog.tsx` close button, and extracted the textarea's base styles into a new `.textarea` utility class in `src/index.css` (mirroring `.input`'s `:focus` visuals) since `.input` is height-locked at 36px and can't be used directly. Updated `NewGameStudyDialog.tsx` to apply `className="textarea font-mono"` and drop the now-redundant inline base styles. `npm run typecheck` clean; `npm test` 37/37 passed. Branch `claude/improve-trainer-new-game-study-dialog-2026052400` pushed; PR #63 opened with the `bot-audit` + `automated` labels. Transport: MCP GitHub Contents API for the run-log commit (direct `git push origin main` returned HTTP 403 from the sandbox proxy); Playwright `chromium` against prod.
