# Audit run — 2026-06-19T03:04:58Z
**Mode:** DISCOVERY
**Subject:** n/a
**Result:** DISCOVERED (+2 -0 kept 30)

All 30 active features had `lastTestedAt > lastDiscoveryAt` (last discovery was 2026-06-14T12:07:03Z and the cycle wrapped with `chore(audit): trainer-games-browser OK` at 2026-06-19T00:09Z), so DISCOVERY mode triggered per rule 2b. Crawled the production site at https://praxis.tsrun.dev/ unauthenticated (`/`, `/tour`) and then signed in as `claude.bot@gmail.com` and crawled `/trainer/studies`, `/trainer/students`, `/trainer/games`, `/settings`, `/student/dashboard`, and `/tournaments`, snapshotting hrefs/buttons/headings/tabs on each. No pageerrors, all routes returned 200.

Two new UI surfaces surfaced that are not yet represented in `features.json`. First, the **Settings page Password card** (`src/auth/SettingsPage.tsx` `PasswordCard`, around the "Change password" button on `/settings`) is a self-contained three-field form (current password / new password / confirm new password) with its own validation (`"New password must be at least 8 characters."`, `"Passwords don't match."`), success banner, and submit-button busy state — distinct from the existing `settings-page` and `settings-api-key-create` features and worth its own rotation. Second, the **Tournaments view toggle** (`src/tournaments/TournamentsPage.tsx:92-93`, `Liste` / `Carte` segmented buttons with `aria-pressed` + `seg-toggle-btn`) swaps the page between `TournamentList` and a lazy-loaded `TournamentMap`; the existing `tournaments-page` feature only smoke-tests the default list, so the map branch is currently uncovered. Added both as `settings-password-change` and `tournaments-view-toggle` (untested, untested-result, no PR). Bumped `lastDiscoveryAt` to 2026-06-19T03:04:58Z and `discoveryCount` 7 → 8. No features marked removed — every previously-listed route still rendered.
