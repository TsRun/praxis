# Audit run — 2026-05-24T12:06:12Z
**Mode:** TEST_ONE
**Subject:** landing-page-render
**Result:** IMPROVED

REVIEW pass first: the only open `bot-audit` PR (#75 — `trainer-studies-list`) already carries a prior review by this bot, so the review queue is empty. Moved to TEST_ONE.

Picked `landing-page-render` (oldest `lastTestedAt` of any feature without an open `bot-audit` PR — the truly oldest, `trainer-studies-list`, is being skipped because PR #75 is still open against it). Wrote `tests/audit/landing-page-render.spec.ts` and ran it against `https://praxis.tsrun.dev/` via `playwright.audit.config.ts`. Page returns 200; no page errors; no app-level console errors; hero / features / auth form all render.

The UI-focused diagnostic block surfaced four actionable observations against prod:

- The sign-in/up form's three inputs (Email, Nickname, Password) have visible `<label>` text but no programmatic association — `id=null`, `labelsFor=0`, no wrapping label, no `aria-label` / `aria-labelledby`. Email is also `type="text"` rather than `type="email"`.
- The account-mode toggle (Sign in / Create account) and the role-picker buttons (Trainer / Student / Solo) render with `aria-pressed=null` and no group semantics — the visual active state isn't surfaced to assistive tech.
- At 375×812 the page horizontally overflows by 7 pixels (`scrollWidth=382` vs `clientWidth=375`). Source is the hero radial-gradient glow positioned `inset: -10%` relative to a wrapper that fills the full hero-grid cell width on mobile; the body had no `overflow-x` containment.

Fix on `claude/improve-landing-page-render-2026052412`: `src/auth/SignInUpForm.tsx` gains `useId`-minted ids on the three inputs with matching `htmlFor` on labels, `aria-pressed` on the mode toggle and role buttons, `role="group"` + `aria-label` / `aria-labelledby` on the two button groups, an aria-described error region (always rendered, `role="alert"` + `aria-live="polite"`), and the email input becomes `type="email"`. `src/index.css` adds `body { overflow-x: clip; }` to contain the hero glow without creating a scroll container. Diff: 6 files, 258 / 13 — of which 215 additions are the new audit spec, so ~40 LOC of production-code change. `npm run typecheck` clean; `npm test` 37/37; `npm run build` clean; audit spec re-passes against prod. PR #78 opened with labels `bot-audit` / `automated`.

Transport: branch pushed via `claude/`-prefixed name, PR created via `mcp__github__create_pull_request`, labels applied via `mcp__github__issue_write` (PRs share the issues endpoint for labels). `features.json` bump for `landing-page-render` is on the PR branch (will land with the merge).
