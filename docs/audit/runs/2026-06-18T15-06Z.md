# Audit run — 2026-06-18T15:06:42Z
**Mode:** TEST_ONE
**Subject:** settings-api-key-create
**Result:** OK+MERGED

Exercised `/settings` API keys flow end-to-end as the bot — opened the New
key dialog, validated empty/whitespace input keeps Mint disabled, minted a
real key (39 chars), copied to clipboard (verified via `navigator.clipboard`),
listed it, then revoked via the confirm dialog. Mobile pass at 375x812
reopened the create dialog with the page free of horizontal overflow
(scrollWidth 360, clientWidth 375) and all dialog buttons rendering at 36px
height — comfortably above the WCAG 2.5.8 AA touch-target floor.

A11y micro-check: dialog has `role="dialog"`, `aria-modal="true"`, and
`aria-labelledby` pointing at the heading; the Label input is wrapped in
a real `<label>` and auto-focuses on open; Mint key shows
`cursor: not-allowed` / `opacity: 0.5` when disabled; Revoke buttons expose
`aria-label="Revoke key \"<name>\""`. No application console errors and no
uncaught page errors. Spec passed in 6.7s. Nothing to fix this rotation.
