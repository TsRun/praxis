# Audit run — 2026-06-06T15:09:00Z
**Mode:** DISCOVERY
**Subject:** n/a
**Result:** DISCOVERED (+3 -0 kept 26)

Cycle complete: all 26 non-removed features had `lastTestedAt > lastDiscoveryAt` (previous lastDiscoveryAt = 2026-06-03T09:15:00Z). Ran a crawl against prod with the trainer bot account, plus an audit of the React Router config in `src/App.tsx`, to surface routes/components not yet represented.

Three new features added:

- `student-game-study-viewer` — `/student/study/game/:id`, the student-side counterpart to `trainer-game-study-editor`. The route exists in the student layout but had no audit coverage.
- `student-tactic-set-viewer` — `/student/study/tactic/:id`, ditto for tactic sets.
- `trainer-assign-study-dialog` — `AssignStudyDialog` is launched from the opening/game study editor via the "Assign to student" primary action and had no entry yet.

`/role-picker` and `/invite/:token` exist as routes but resolve to redirects or stub pages for an already-onboarded bot account and aren't useful audit targets right now. `/trainer/studies/opening/:id/import` reuses the `ImportGamesPage` already covered by `trainer-import-lichess`. No features were removed.

`lastDiscoveryAt` bumped to 2026-06-06T15:05:18Z, `discoveryCount` incremented to 5. Existing testing history preserved on the 26 retained features.
