# Audit run — 2026-05-24T05:04:36Z
**Mode:** REVIEW
**Subject:** PR #66 — `trainer-new-study-menu` (ARIA menu-button + Escape)
**Result:** APPROVED (posted as COMMENT — GitHub blocks self-approval on the bot account)

Four `bot-audit` PRs were open at the start of the run (#66, #64, #63, #61), well under the 5-PR queue-full ceiling. #61, #63, and #64 already carry a prior verified-ready-to-ship review from this bot, so #66 was the oldest un-reviewed one and became the REVIEW target.

Checked the PR branch out via `pull/66/head`. `npm run typecheck` returned clean output and `npm test` reported 6 files / 37/37 tests passing. Re-ran the matching audit spec `tests/audit/trainer-new-study-menu.spec.ts` against prod via `playwright.audit.config.ts` — it passed in 7.4s and its diagnostics independently reproduce all four OBSERVED issues the PR claims: trigger `aria-haspopup`/`aria-expanded`/`aria-controls` are all null on prod, the three menu items render with `role: null`, the menu container `<div>` has no `role="menu"` or accessible name, and `ESCAPE CLOSES MENU: false`. Mobile 375x812 still fits at `width=320` inside the 375 viewport, no overflow.

The diff (`src/trainer/StudiesPage.tsx`, ~25 LOC of production change plus a 201-LOC new audit spec) maps 1:1 onto those four findings: trigger gains the menu-button ARIA triad and an `id`, the dropdown `<div>` gains `role="menu"` + `aria-labelledby`, each `NewStudyMenuItem` button gets `role="menuitem"`, and the existing `useEffect` (guarded by `if (!menuOpen) return;`) adds a `keydown` Escape handler that closes the menu and restores focus to the trigger via `getElementById`. Cleanup tears down both `mousedown` and `keydown` listeners together — no leak, no listener when the menu is closed. `Btn` (`src/components/ui/atoms.tsx:31-47`) extends `ButtonHTMLAttributes` and spreads `...rest` onto the underlying `<button>`, so the new `id` and `aria-*` props forward correctly. Purely additive — no visual, layout, or behavior changes for mouse users. 3 files / 229 LOC, no forbidden paths touched. Safely within the 300 LOC / 8 file ceiling.

Decision: approve. Posted as a `COMMENT` review on PR #66 (the bot identity authored the PR, and GitHub blocks self-approval) with the typecheck + test + audit-spec evidence and an explicit "Safe to merge" verdict, matching the established pattern from PR #61/#63/#64 reviews. No `features.json` mutation (REVIEW runs don't bump feature state). Transport: GitHub MCP for the review write; Playwright over HTTPS to https://praxis.tsrun.dev for the prod re-verification.
