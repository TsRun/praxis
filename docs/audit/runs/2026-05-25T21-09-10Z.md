# Audit run — 2026-05-25T21:09:10Z
**Mode:** REVIEW_AND_MERGE
**Subject:** PR #107
**Result:** APPROVED+PENDING (auto-merge declined — daily cap)

Reviewed [#107 "[bot] Improve Profile menu dropdown"](https://github.com/TsRun/praxis/pull/107) on branch `claude/improve-profile-menu-2026052518`. The PR adds Escape-to-close, `aria-controls`/`aria-label` on the trigger, and `id`/`aria-label` on the popup in `src/auth/UserMenu.tsx` — straightforward WAI-ARIA menu-button conformance fixes. Diff is 4 files / 254 LOC (`src/auth/UserMenu.tsx`, `tests/audit/profile-menu.spec.ts`, `docs/audit/features.json`, one new run log). No forbidden write paths touched: `src/auth/` is the frontend module; the guardrail is `server/auth/`.

Verification on the PR branch: `npm run typecheck` ✓, `npm test` ✓ (37/37 in 3.11s), and `npx playwright test tests/audit/profile-menu.spec.ts` against prod ✓ (1 passed, 6.8s) — the probe re-confirmed the bugs on the still-deployed build (`ariaControls: null`, `CLOSES ON ESC: false`) and reported zero page/app-console errors plus no mobile horizontal overflow (scrollWidth 360 ≤ 375).

Auto-merge declined for this run: today already has 6 merged `fix(audit):` commits on `origin/main` (PRs #75, #78, #81, #86, #89, #93) versus the policy ceiling of 4 per UTC day. GitHub also refuses APPROVE on a PR opened by the same identity, so the review was filed as COMMENT with the full verification summary; the PR is left open for a human to squash-merge.
