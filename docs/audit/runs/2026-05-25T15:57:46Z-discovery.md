# Audit run — 2026-05-25T15:57:46Z
**Mode:** DISCOVERY
**Subject:** n/a
**Result:** DISCOVERED (+4 -0 kept 15)

Triggered because `docs/audit/features.json` was schema v1 (no `lastDiscoveryAt` /
`discoveryCount`), which per spec is treated as `lastDiscoveryAt = 1970-01-01Z` and
`discoveryCount = 0`. All 15 existing non-removed features had `lastTestedAt` set
after that sentinel, so the cycle was considered complete — and the 30-day sanity
guard also fired.

Crawl ran against prod via `tests/audit/_discovery.spec.ts`. Captured snapshots for
public routes `/`, `/tour`, `/role-picker` and authed routes `/trainer/studies`,
`/trainer/students`, `/trainer/games`, `/student/dashboard`, `/settings` (raw at
`docs/audit/_crawl_raw.json`, gitignored). A follow-up editor-routes crawl
(`_discovery_editors.spec.ts`) timed out on the heavy editor pages so the dynamic
study editor routes are not yet first-class features — leaving them for a future
discovery pass.

All 15 existing features were verified still present in the crawl and kept verbatim
(`removed: false`). The schema was promoted to v2 with `lastDiscoveryAt`,
`discoveryCount`, `site`, and per-feature `removed` fields.

Four new behaviours observed in the crawl that were not represented in the
inventory were added with `lastTestedAt: null`:

- `profile-menu` — the avatar/name button in the top-right header (visible on
  every authed page snapshot as `CB Claude Bot`) opens a `role="menu"` popover
  with the user's roles and a sign-out action.
- `trainer-students-filters` — the All / Linked / Invited tabs on
  `/trainer/students` are interactive filter controls distinct from the page
  render itself.
- `student-dashboard-assignments-tabs` — the Active / Completed buttons under
  the dashboard assignments list switch tabs.
- `settings-api-key-create` — the `New key` button in the API keys section of
  `/settings` triggers key creation, separable from the general settings render.

Counts: kept 15, newly added 4 (`profile-menu`, `trainer-students-filters`,
`student-dashboard-assignments-tabs`, `settings-api-key-create`), marked removed 0.
