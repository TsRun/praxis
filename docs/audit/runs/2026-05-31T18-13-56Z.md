# Audit run — 2026-05-31T18:13:56Z
**Mode:** TEST_ONE
**Subject:** settings-api-key-create
**Result:** IMPROVED+PENDING+REASON

Exercised the API keys creation flow on /settings: clicked "New key", confirmed the dialog (`role=dialog`, `aria-modal=true`, auto-focused input wrapped in `<label>Label`), verified Mint button disabled on empty/whitespace input, submitted `audit-<stamp>`, asserted the minted dialog appeared with a 39-char token, validated Copy → "Copied" → clipboard contents, dismissed Done, confirmed the new row rendered and the revoke icon button carried both `title` and `aria-label`. Cleaned up by revoking the freshly-minted key and re-checking the row was gone. Repeated open at 375×812 — no horizontal overflow (scroll 360 / client 375), dialog 345×193 fits cleanly. No `pageerror` events; no application-level console errors.

UI quality finding: the per-key revoke icon-only button measured 25×25 px (padding 6, icon 13). That's right at the WCAG 2.5.5 AA target-size floor (24×24) and well under the 44×44 recommendation — uncomfortably small to hit on touch, especially since it's the destructive action on each row. Bumped `padding: 6 → 6 with minWidth/minHeight 32`, raised the icon to 14, added `display: inline-flex` + centred alignment + `borderRadius: 6` so the focus/hover affordance has shape. Tap target is now ~33×33 px while the visual weight stays similar inside the row. Bookkeeping: features.json `lastTestedAt`/`lastResult` updated; PR auto-merge pending verification.
