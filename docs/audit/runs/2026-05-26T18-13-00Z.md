# Audit run — 2026-05-26T18:13:00Z
**Mode:** TEST_ONE
**Subject:** trainer-new-opening-study-dialog
**Result:** IMPROVED+PENDING

Ran the existing `tests/audit/trainer-new-opening-study-dialog.spec.ts` against prod (`e8cce59`). Spec passed but logs surfaced a real a11y bug in the **SideOption** buttons (White / Black side picker inside the dialog):

```
SIDE OPTIONS: [
  { title: "WhiteYou're preparing 1.e4 / 1.d4 / etc.", ariaPressed: 'true',
    ariaLabel: null, role: null, tabIndex: 0 },
  { title: "BlackYou're preparing replies to White's first move.", ariaPressed: 'false',
    ariaLabel: null, role: null, tabIndex: 0 },
]
```

The button's accessible name is computed from its inner text. The visible UI uses two stacked `<div>`s — one for `title` ("White") and one for `hint` ("You're preparing 1.e4 / 1.d4 / etc.") — with no whitespace between them. The flat text content collapses to `WhiteYou're preparing 1.e4 / 1.d4 / etc.` (and likewise `BlackYou're…`), which is what AT will announce: a mashed run-on word with no boundary between the side label and the explanation.

Everything else the spec exercised was sound on prod and is unchanged:
- Dialog has `role="dialog"` already (uses shared `src/components/ui/Dialog.tsx`). ✓
- Close button: `aria-label: "Close"`. ✓
- Name input has an implicit wrapping `<label>` "Study name". ✓
- `:focus-visible` outline on name input and both side options: solid 2 px amber outline, 2 px offset. ✓
- Keyboard tab order: name input → first side option → second side option (`matchesFocusVisible: true`). ✓
- Create button disabled when name empty, enabled when filled. ✓
- Mobile 375×812: dialog 345 px wide, side options stack vertically (full 301 px width). ✓
- Page errors: `[]`. App console errors: `[]`.

Fix (`src/trainer/NewOpeningStudyDialog.tsx`): add `aria-label={`${title} — ${hint}`}` to the `<button>` in `SideOption`. The visible UI is unchanged; the accessible name becomes `White — You're preparing 1.e4 / 1.d4 / etc.` (and similarly for Black). Single line, +1 LOC.

Verification on PR branch `claude/improve-trainer-new-opening-study-dialog-2026052618`:
- `npm run typecheck` ✓
- `npm test` ✓ (37/37)
- Audit spec still passes against prod (the fix only changes the rendered DOM after deploy; the spec already doesn't fail on the bug).
