# Audit run — 2026-05-31T21:13:39Z
**Mode:** TEST_ONE
**Subject:** student-dashboard-assignments-tabs
**Result:** OK+MERGED

Exercised the Active/Completed segmented control inside the "All assignments" card on /student/dashboard. Confirmed the group carries `role="group"` + `aria-label="Filter assignments by state"`, both buttons carry `aria-pressed` reflecting the current state, and toggling sets/removes the `.active` class on the right tab. Verified contrast: active tab text `rgb(236,236,239)` on a faint `rgba(255,255,255,0.06)` chip, inactive tab text `rgb(160,160,168)` on transparent — both comfortably above WCAG-AA against the dark card. Keyboard tabbing lands on the buttons and triggers `:focus-visible` with a 2px amber outline at +2px offset. Space activates the focused tab. Mobile pass at 375×812: no horizontal overflow (scroll 360 / client 375); the header row wraps the segmented control under the heading via `flexWrap: wrap`. No `pageerror` events; no application-level console errors. Decision: OK, bump `lastTestedAt`.
