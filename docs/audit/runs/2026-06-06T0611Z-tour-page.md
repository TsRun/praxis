# Audit run — 2026-06-06T06:11:00Z
**Mode:** TEST_ONE
**Subject:** tour-page
**Result:** IMPROVED+PENDING+PR

On `/tour`, the scene-tab strip uses three label colors: active = `--accent`, past = `--text-dim` (#a0a0a8), future = `--text-faint` (#6b6b73). At 12.5px on `--page-bg` (#0a0a0c), `--text-faint` lands at a measured **3.74:1** contrast — below WCAG AA's 4.5:1 for normal text. Since the scene chips are clickable navigation (the user can jump to any scene), this is a real readability problem, not a decorative case. Confirmed on prod via Playwright by inspecting each unpressed button's computed color/background.

**Fix:** in `src/marketing/TourPage.tsx`, collapse the past/future split — non-active scene tabs now use `--text-dim` uniformly (~7.84:1 contrast, comfortably AA). Visual hierarchy is preserved by the active tab's amber color, accent ring, and `--accent-soft` background. The `past` variable becomes unused and is removed.

Typecheck + 48 unit tests pass. Audit spec passes against prod on the PR branch (contrast is logged, not strictly asserted, because the spec runs against the not-yet-deployed prod build).
