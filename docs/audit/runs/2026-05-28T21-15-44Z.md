# Audit run — 2026-05-28T21:15:44Z
**Mode:** TEST_ONE
**Subject:** trainer-students-filters
**Result:** IMPROVED+PENDING+VERIFICATION

On /trainer/students the All / Linked / Invited segmented control toggled
its `aria-pressed` and `.active` class correctly, but the `filter` state
was never consumed by the row-filtering logic: `(rows ?? []).filter(s =>
s.name.toLowerCase().includes(q.toLowerCase()))`. Clicking "Invited"
would silently show the same linked-only roster, which is misleading the
moment a trainer has any linked students. With the test account at zero
students the visible breakage today is the "No students yet" empty state
remaining identical on every tab (the heading should reflect which tab
is empty).

Smallest fix in `src/trainer/StudentsPage.tsx`:
- The list filter short-circuits to an empty roster when `filter ===
  'invited'` — honest with the current `/api/trainer/students` shape,
  which only returns linked rows.
- `EmptyStudents` now takes the active `filter` and renders a tab-aware
  heading + body (`No invited students` / `No linked students yet` /
  `No students yet`), and gets `role="status"` + `aria-live="polite"`
  so screen readers announce the change when tabs switch.
- Desktop and mobile layouts are unchanged.

Typecheck and `npm test` (37 tests) pass on the branch. The audit spec
passes against prod (it logs the new empty-state heading rather than
hard-expecting it, so it keeps passing through the deploy gap).
