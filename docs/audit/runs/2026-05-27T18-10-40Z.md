# Audit run — 2026-05-27T18:10:40Z
**Mode:** TEST_ONE
**Subject:** signup-form-render
**Result:** OK

Picked oldest feature `signup-form-render` (last tested 2026-05-24T17:13:48Z). Ran the audit spec against prod at viewport 1280 and 375×812; passed cleanly — no page errors, no app console errors, no horizontal overflow on mobile (`scrollWidth=360` vs `clientWidth=375`).

Observed UI quality:
- Mode toggle (`Sign in` / `Create account`) correctly mirrors `aria-pressed`; clicking `Create account` swaps state and reveals the Nickname field and role-picker grid.
- All three signup inputs (email/nickname/password) are programmatically labelled via `<label for>` (`labelsFor=1` each), with sensible `autocomplete` values (`email`, `nickname`, `new-password`).
- Role-picker buttons (`Trainer / Student / Solo`) each carry both `aria-pressed` and a descriptive `aria-label` (`"Trainer — I coach others"`, etc.); default selection is `Trainer` (pressed), matching the rendered yellow accent.
- Email input focus state shows a 2 px solid amber outline plus a faint ring shadow — clearly visible.
- At 375 × 812 the `.grid-3` collapses to single-column (each tile 280×96 px), well above the 40 px tap-target minimum; the rest of the form stacks naturally with no clipping.

No improvement required — leaving as OK and bumping `lastTestedAt`.
