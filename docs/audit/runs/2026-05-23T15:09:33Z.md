# Audit run: 2026-05-23T15:09:33Z

**Mode:** TEST_ONE  
**Feature tested:** `landing-page-render` (route `/`)  
**Result:** ok

## What happened

The Playwright spec visited the production landing page at `https://praxis.tsrun.dev/`. All visual assertions passed: the H1 headline rendered, the three feature card headings (Opening trees, Game studies, Spaced repetition) were visible, and the sign-in form's email input (`placeholder="you@studio.club"`) and password input were visible after scrolling to the `#auth` section. A full-page screenshot was captured to `tests/audit/screenshots/landing-page-render.png`.

## Observation noted (not actionable)

One console error was captured by the browser: `Failed to load resource: the server responded with a status of 401 ()` from `https://praxis.tsrun.dev/api/auth/me`. This is a browser-generated message triggered when `AuthContext` checks the auth state of an unauthenticated visitor. The fix would require changing the server's `/api/auth/me` response from `401` to `200 { user: null }` when no session is present — this is an auth server route and is off-limits per the audit agent's hard guardrails. The page renders correctly and no functional issues were observed.
