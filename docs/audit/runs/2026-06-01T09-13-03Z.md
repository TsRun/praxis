# Audit run — 2026-06-01T09:13:03Z
**Mode:** TEST_ONE
**Subject:** trainer-new-game-study-dialog
**Result:** OK+MERGED

Exercised the **New game study** dialog on `/trainer/studies` (open the
**New study** menu, click **Game study**) against deployed prod, at
desktop 1280×800 and mobile 375×812. Login OK, dialog opens, dialog
title `New game study` is exposed via `role=dialog`/`aria-label`, and the
close button has `aria-label="Close"` (text content empty — aria-label
doing real work).

UI lens, all green:
- **Labels**: both the `Study name` input and the `PGN` textarea are
  wrapped in a parent `<label>` (`wrappedInLabel: true`); SR-readable.
- **Focus indicator (name input)**: settled `:focus` state matches the
  amber convention — `border-color: rgb(251, 191, 36)` + `box-shadow:
  rgba(251, 191, 36, 0.12) 0 0 0 3px`, plus the global `:focus-visible`
  2px amber outline.
- **Focus indicator (PGN textarea)**: the captured `getComputedStyle`
  read fires in the first frame after `.focus()`, mid-120 ms transition,
  so the spec saw the unsettled border/box-shadow but the global 2px
  `:focus-visible` outline was applied immediately. The `.textarea:focus`
  rule (`src/index.css:213`) is identical to `.input:focus` and applies
  the same amber border + soft box-shadow once the transition settles —
  matches what prior runs observed.
- **Empty-form gating**: `Import game` is correctly `disabled` while
  either field is empty.
- **Mobile 375×812**: dialog box `x=7.5, y=179, w=345, h=454` — fits
  inside the viewport with ~10 px gutter each side from `max-width:
  92vw` and `max-height: calc(100dvh - 32px) + overflowY: auto`.
- **Page errors**: `[]`. **App console errors**: `[]` (resource-load
  noise filtered).

No improvement warranted. Marking OK and rotating.
