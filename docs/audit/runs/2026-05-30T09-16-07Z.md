# Audit run — 2026-05-30T09:16:07Z
**Mode:** TEST_ONE
**Subject:** signup-form-render
**Result:** IMPROVED+PENDING

Picked oldest-tested live feature `signup-form-render` (lastTestedAt 2026-05-27T18:10:40Z, which predates the last discovery). Wrote a UI/a11y spec that opens the landing page, switches the form to "Create account" mode, and verifies the rendered inputs and role picker against prod.

Spec passed against prod: all three inputs (email, nickname, password) have associated `<label for="…">`, mode-toggle and role buttons expose `aria-pressed`, all three role tiles toggle correctly, no horizontal overflow at 375×812 (scrollWidth 360 vs clientWidth 375), no page errors, no application console errors.

Observation that drove the fix: clicking the submit button with the form blank made an API round-trip and surfaced a generic server-side error "missing fields" inside the `role="alert"` region — `email`/`nickname`/`password` inputs had no `required` attribute, so the browser never blocked submission. Likewise no `minLength` on the signup password despite the placeholder reading "at least 8 characters".

Fix (one file, `src/auth/SignInUpForm.tsx`): added `required` to the email, nickname, and password inputs, plus `minLength={8}` on the password in signup mode. The browser now blocks empty submissions and the constraint matches the placeholder copy — no behavioral change for valid input.

Typecheck + 37 unit tests green. PR opened.
