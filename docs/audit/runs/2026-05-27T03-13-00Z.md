# Audit run — 2026-05-27T03:13:00Z
**Mode:** TEST_ONE
**Subject:** trainer-new-tactic-set-dialog
**Result:** IMPROVED+PENDING+VERIFICATION

Picked oldest feature `trainer-new-tactic-set-dialog` (last tested 2026-05-24T06:16:01Z). Ran the existing audit spec against prod; it passed cleanly (no page errors, no app console errors). UI observations: dialog has `role="dialog"`/`aria-modal="true"`/`aria-label="New tactical set"`, name input has a wrapping `<label>` with `htmlFor`, `required`, `aria-required`, the create button is disabled when empty, focus ring is amber 2px outline + box-shadow, close button has `aria-label="Close"`, and helper meta text contrast is 7.39:1 against `--card-bg` (passes AAA). Mobile pass at 375×812 fit the dialog cleanly (345px wide).

One real observed a11y gap: pressing Tab from the last focusable inside the modal (Create set) moved focus to `BODY` — the modal had no focus trap, so keyboard users could tab into content behind the dialog while `aria-modal="true"` is set. Smallest-change fix: add a Tab/Shift+Tab handler scoped to the dialog root in `src/components/ui/Dialog.tsx` that wraps focus between the first and last focusable element, and restore focus to the previously focused element on close. The change is contained to the shared Dialog component, benefits all 8 callers (NewTacticSetDialog, NewOpeningStudyDialog, NewGameStudyDialog, ConfirmDialog, SettingsPage, EditRootDialog, AssignStudyDialog, ImportLichessDialog), and is ~30 LOC. Typecheck + unit tests pass.
