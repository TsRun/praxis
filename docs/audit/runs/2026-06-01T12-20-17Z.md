# Audit run — 2026-06-01T12:20:17Z
**Mode:** TEST_ONE
**Subject:** trainer-new-opening-study-dialog
**Result:** IMPROVED+PENDING (PR to follow)

Ran the existing audit spec against prod (https://praxis.tsrun.dev). Sign-in, dialog open, a11y/focus/console probes — no console errors, no page errors, labels and aria states correct, focus rings present, touch targets fine at 375x812.

A separate probe captured `getBoundingClientRect()` for the dialog and the **Create study** button at both 1280×720 and 375×812. The dialog has `overflowY: auto` with `max-height: calc(100dvh - 32px)`, and its scrollHeight (788 desktop / 846 mobile) exceeds clientHeight (688 / 780) on both viewports. The primary CTA renders at y=746–782 on desktop (viewport 720) and y=804–840 on mobile (viewport 812) — **below the visible fold** on both. The optional "Starting position" board pushes the action buttons out of view on initial render, so users must scroll inside the dialog to reach Cancel/Create.

Fix in `src/trainer/NewOpeningStudyDialog.tsx`: make the button row `position: sticky; bottom: 0` with negative inline margins to span the dialog's padding, a `borderTop` hairline for visual separation, and `background: var(--card-bg)` so scrolling content reads cleanly underneath. Behavior is unchanged when the dialog content fits — sticky elements only activate when overflow would otherwise hide them.

`npm run typecheck` and `npm test` both pass.
