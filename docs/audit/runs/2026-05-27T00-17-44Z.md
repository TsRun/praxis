# Audit run — 2026-05-27T00:17:44Z
**Mode:** TEST_ONE
**Subject:** trainer-new-study-menu
**Result:** IMPROVED+PENDING+PR

Audit spec on prod passed: trigger has correct ARIA (`aria-haspopup`/`aria-expanded`/`aria-controls`), menu has `role="menu"` + `aria-labelledby`, three items present (Opening study, Game study, Tactical set), Escape closes, outside click closes, mobile 375×812 layout fits (menu 320px wide, left:16 right:336, no horizontal overflow), dialog opens on item click, no console errors.

UI gap observed: when the menu opens via mouse click, focus stays on the trigger button (logged `FOCUSED after open: text="New study"`) and pressing ArrowDown on the trigger does nothing (`FOCUSED after ArrowDown` unchanged). Per ARIA APG menu-button pattern, opening the menu should move focus to the first menuitem, and Arrow keys should navigate among items. Improvement adds: (1) auto-focus first menuitem on open, (2) ArrowDown/ArrowUp/Home/End keyboard navigation within the menu, (3) ArrowDown on the trigger opens the menu. Escape already restored focus to trigger — kept that behavior, added `preventDefault`. Diff is in `src/trainer/StudiesPage.tsx` only.
