# Audit run — 2026-05-27T09:14:00Z
**Mode:** TEST_ONE
**Subject:** trainer-studies-list
**Result:** IMPROVED+PENDING+VERIFICATION

Picked oldest feature `trainer-studies-list` (last tested 2026-05-24T10:09:41Z). Ran the existing audit spec against prod; it passed (no page errors, no app console errors). A11y is in good shape: search input has `aria-label="Search studies"`, filter segmented group exposes `role="group"` + `aria-label="Filter studies by type"` with `aria-pressed` on each button, view-toggle icons have `aria-label="Grid view" / "List view"`, the New-study trigger advertises `aria-haspopup="menu"` / `aria-expanded` / `aria-controls`, and focus rings are visible (2px amber outline + soft glow). Mobile (375×812) fits cleanly (scrollWidth 360 within 375 viewport, head buttons total 326px wide, no horizontal overflow).

Real observed UI gap: the Opening studies section is currently broken on prod — the API returns a SQL error and the Section renders raw `column os.root_pgn does not exist` text right next to a small underlined "Retry" link styled as inline text. The error block uses the dim `.meta` typography, has no border / background / icon, no `role="alert"`, and the retry control is easy to miss visually. SQL itself is off-limits (server/db), but the frontend rendering is clearly in scope and below the rest of the app's visual quality. Smallest-change fix: in `src/trainer/StudiesPage.tsx`'s `Section` component, replace the bare `.meta` div with a styled alert box (danger-tinted bg/border, rounded, `role="alert"`, "Couldn't load." prefix in `var(--danger)` + raw message in `var(--text-dim)`) and swap the inline text Retry for a `btn btn-sm btn-danger` button. ~25 LOC, one file, no API or dependency changes. Typecheck + unit tests pass.
