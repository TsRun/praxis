# Audit run — 2026-05-26T00:11:52Z
**Mode:** TEST_ONE
**Subject:** settings-api-key-create
**Result:** OK+MERGED

Exercised the "New key" creation flow in `/settings` → API keys card. Trigger button opens a modal `<Dialog>` rendered via portal with `role="dialog"`, `aria-modal="true"`, `aria-label="New API key"`. The label input is wrapped in a `<label>…<span>Label</span><input/></label>` (implicit labelling, valid per HTML spec), autofocused on open. The "Mint key" submit button is correctly `disabled` while the name is empty and enables once filled. Cancel and the header close button both dismiss; Escape also closes (verified `CLOSES ON ESC: true`).

Numbers: HTTP 200, `pageErrors=[]`, app `consoleErrors=[]`. Trigger focus shows a 2px solid `rgb(251, 191, 36)` (amber) outline. Label-text contrast against the card background = **7.09** (WCAG-AAA). Mobile 375×812: `scrollWidth=360 ≤ 375` (no horizontal overflow); dialog box `x=7.5, y=309, width=345, height=193` — fits with the 92vw `max-width` cap. No improvable UI issues observed in this run; logging the pass and rotating the queue.

PR #107 (profile-menu) still open from the previous run — daily cap on 2026-05-25 blocked auto-merge; left for a human.
