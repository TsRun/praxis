# Audit run — 2026-05-28T15:19:41Z
**Mode:** TEST_ONE
**Subject:** settings-api-key-create
**Result:** OK

Exercised the API-keys "New key" creation flow on /settings end-to-end on prod: opened the New key dialog, verified the label `<Field>` wraps the input implicitly (placeholder "e.g. Claude Code MCP", autofocused, focus indicator is the 2px amber outline plus an amber border + 3px box-shadow halo), confirmed the Mint key button is correctly disabled on empty and on whitespace-only input, minted a key with a unique `audit-bot <timestamp>` label, verified the "Key minted: …" dialog renders the bearer token (39 chars, starts `praxis_…`) and the `claude mcp add` snippet, closed it with Done, then revoked the key via the trash button — confirm dialog uses the destructive red colour (`rgb(248,113,113)`) and removes the row from the list on confirm. Mobile pass at 375×812: `scrollWidth=360 ≤ clientWidth=375` so no horizontal overflow, dialog `boundingBox.width=345` fits cleanly inside the viewport, Cancel + Mint key buttons stay side-by-side. No `pageerror`s, no application console errors (filtering the waived "Failed to load resource" noise).

A11y micro-check passed across all interactive elements: the close (X) button has `aria-label="Close"` and is icon-only (single SVG child, `aria-hidden` via the shared `makeIcon` factory); the trash/revoke buttons carry `aria-label="Revoke key \"<name>\""`; the error span in `NewKeyDialog` uses `role="alert" aria-live="polite"`. Test also includes best-effort cleanup that revokes any leftover `audit-bot …` keys (one was cleaned up this run from an earlier transient run).
