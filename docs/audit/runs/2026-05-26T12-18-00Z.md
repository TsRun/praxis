# Audit run — 2026-05-26T12:18:00Z
**Mode:** TEST_ONE
**Subject:** trainer-invite-student
**Result:** IMPROVED+PENDING

Ran the audit spec for the **Invite student dialog** against prod. The dialog
itself works, but it is a hand-rolled modal (predating `src/components/ui/Dialog.tsx`)
and is missing the keyboard / screen-reader plumbing that the shared `Dialog`
component already provides.

Spec captured on the unmodified prod build (commit `76a6e6a`):

- `DIALOG A11Y: { role: null, ariaModal: null, ariaLabelledBy: null, titleId: null }`
  — the dialog container has no `role="dialog"`, no `aria-modal`, no
  `aria-labelledby`, so screen readers don't recognise it as a modal and
  cannot announce its title.
- `CLOSES ON ESC: false` — keyboard-only users have no way to dismiss the
  dialog except tabbing to the Cancel/Close button.
- `EMAIL id: invite-email labels-for: 1` — the Email field is already
  properly labelled. ✓
- `NICK aria-label: Student nickname` — the nickname field already has an
  accessible name. ✓
- `MOBILE OVERFLOW: { scroll: 360, client: 375 }` — no horizontal page
  overflow at 375×812. ✓
- Page errors `[]`, app console errors `[]`. ✓

Fix in `src/trainer/InviteStudentDialog.tsx` (matches the shared `Dialog`
component's pattern):

- `useEffect` adds a document `keydown` listener that calls `onClose()` on
  `Escape` (cleans up on unmount).
- Dialog panel gets `role="dialog"`, `aria-modal="true"`,
  `aria-labelledby="invite-student-title"`.
- The `<h2>` gets `id="invite-student-title"` so the panel's `aria-labelledby`
  resolves to "Invite a student".

No layout, copy, or visual changes. The "Close" `IconX` button already had
`aria-label="Close"`.

Verification on the PR branch:
- `npm run typecheck` ✓
- `npm test` ✓ (37 passed)
- `npx playwright test tests/audit/trainer-invite-student.spec.ts` ✓ against
  prod (the spec logs the broken state pre-fix; will log the fixed state
  once the PR deploys).
