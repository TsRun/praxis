# Audit run — 2026-05-24T06:16:01Z
**Mode:** TEST_ONE
**Subject:** trainer-new-tactic-set-dialog
**Result:** IMPROVED

Started by checking the REVIEW queue. Four bot-audit labeled open PRs (#61, #63, #64, #66) — all four already had a "Audit review — verified, ready to ship" review from the bot account, so no REVIEW pass was due. Open bot-audit PR count is 4, under the 5-PR queue limit, so the run continued into TEST_ONE.

Picked `trainer-new-tactic-set-dialog` (the oldest feature with `lastTestedAt: null` that isn't blocked by an open PR — `trainer-new-study-menu`, `trainer-new-opening-study-dialog`, `trainer-new-game-study-dialog`, and `trainer-invite-student` all have open improvement PRs against them, so they were skipped; alphabetical order on the remaining three put `trainer-new-tactic-set-dialog` first).

Wrote `tests/audit/trainer-new-tactic-set-dialog.spec.ts` covering login → New study → Tactical set, then probing the dialog accessibility surface (dialog attrs, name-input a11y including required/aria-required/aria-invalid/aria-describedby, focus styles, close button, helper text contrast, form validation, keyboard tab order, mobile 375x812 layout, live-region inventory). Ran it against prod with the audit Playwright config: `1 passed in 6.6s`. `PAGE ERRORS: []`, `APP CONSOLE ERRORS: []`.

Two clear UI/a11y gaps emerged:

1. The Set-name input is required (the Create button is `disabled` while the field is empty) but the input itself reports `required: false, ariaRequired: null` and the visible "Set name" label has no required marker. Screen reader users discover the required state only by trying to submit.
2. `DIALOG LIVE REGIONS: []` — the error span rendered on server-error paths (`<span>{err}</span>`) has no `role="alert"` / `aria-live`, so assistive tech is not notified when an error appears.

Several other things were verified intact and intentionally left untouched: dialog has `role="dialog"` + `aria-label="New tactical set"`; name input `:focus` indicator is solid 2px accent outline + 3px accent-soft shadow + accent border (good); helper `.meta` text passes WCAG AA at 6.98:1 (`#a0a0a8` on `#141416`, 13px); mobile 375x812 fits cleanly (dialog 345 wide, no horizontal page overflow). The shared Dialog close (X) button still has `ariaLabel: null` on prod, but that's a shared-component concern already being fixed by open PR #63 — left out of this PR to avoid a merge conflict.

Branched `claude/improve-trainer-new-tactic-set-dialog-2026052406` and applied a scoped fix in `src/trainer/NewTacticSetDialog.tsx`: added `id="tactic-set-name"` + `htmlFor`, `required` + `aria-required="true"` on the input, a visible `*` required-marker glyph (aria-hidden) next to "Set name", conditional `aria-invalid` + `aria-describedby` wired to the error span, and `id="tactic-set-name-error"` + `role="alert"` on the error span. Purely additive a11y attributes plus one visible glyph — no layout or behavior changes for mouse users.

Verification: `npm run typecheck` clean; `npm test` → 6 files, 37/37 passed. Diff scope: 3 files / 244 insertions / 5 deletions (220 of those additions are the new audit spec; production-code change is ~25 LOC). No forbidden paths touched.

Opened PR #69 (https://github.com/TsRun/praxis/pull/69, labels `bot-audit` + `automated`). Subscribed to PR activity per the post-creation prompt.
