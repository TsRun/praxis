# Audit run — 2026-05-24T04:12:09Z
**Mode:** TEST_ONE
**Subject:** trainer-new-study-menu
**Result:** IMPROVED

Picked `trainer-new-study-menu` — oldest `lastTestedAt: null`, no open `bot-audit` PR against its id. (The three sibling feature ids with earlier alphabetical sort — `trainer-invite-student`, `trainer-new-game-study-dialog`, `trainer-new-opening-study-dialog` — were skipped because PRs #61, #63, #64 are still open against them.)

Wrote `tests/audit/trainer-new-study-menu.spec.ts` and ran it against `https://praxis.tsrun.dev/trainer/studies` via Playwright + chromium. The first run failed: the email placeholder on the sign-in form is `you@studio.club`, not `you@example.com`, and a previous stale `.auth/bot.json` was masking landing-page renders behind a `/trainer/studies` URL. Updated the spec to match the existing PR-#61 sign-in helper (form with "Continue with Google" → `getByPlaceholder('you@studio.club')`) and to always re-login fresh; that produced a clean 1-pass run with `PAGE ERRORS: []` and no app-level console errors.

Diagnostics on the actually-rendered dropdown showed four WAI-ARIA menu-button gaps: the trigger had no `aria-haspopup` / `aria-expanded` / `aria-controls` in either state, the three menu items were plain `<button>`s with `role: null`, the popup `<div>` had `parentRole: null` with no accessible name, and pressing `Escape` did not close the menu (`ESCAPE CLOSES MENU: false`). On the positive side, `Tab` from the trigger lands on the first item with a visible `:focus-visible` outline (`solid 2px rgb(251,191,36)`), and the mobile 375x812 popup fits cleanly (`x=16, width=320` inside a `viewportWidth=375`) — both intentionally left alone.

Decided IMPROVE. Created branch `claude/improve-trainer-new-study-menu-2026052404` from main and made a small additive diff to `src/trainer/StudiesPage.tsx`: trigger `Btn` gets `id="new-study-trigger"` plus `aria-haspopup="menu"` / `aria-expanded={menuOpen}` / `aria-controls="new-study-menu"`; the popup `<div>` gets `id="new-study-menu"` / `role="menu"` / `aria-labelledby="new-study-trigger"`; the `NewStudyMenuItem` `<button>` gets `role="menuitem"`; the menu-lifecycle `useEffect` grows a `keydown` listener that closes on `Escape` and returns focus to the trigger (using `getElementById` because the `Btn` atom doesn't forward refs). No visual or behavior change for mouse users.

`npm run typecheck` clean. `npm test` 37/37 passing. Diff: 3 files / 226 insertions / 4 deletions (well under the 300 LOC / 8 file budget; the bulk of the additions are the gitignored audit spec, force-added with `git add -f`). No forbidden paths touched.

Pushed `claude/improve-trainer-new-study-menu-2026052404` and opened PR [#66](https://github.com/TsRun/praxis/pull/66) (`bot-audit`, `automated` labels applied via `issue_write` after creation; the `create_pull_request` MCP tool doesn't accept labels directly).

Transport: Playwright `chromium-headless-shell` (1223) against `https://praxis.tsrun.dev` over the sandbox's outbound network.
