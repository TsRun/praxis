# Audit run — 2026-05-25T06:06:17Z
**Mode:** SKIPPED
**Subject:** n/a
**Result:** SKIPPED

Queue full: 6 open PRs labeled `bot-audit` (#75, #78, #81, #86, #89, #93), which exceeds the hard guardrail of >5. Per the spec, this run is skipped to avoid piling on duplicate PRs while the rotation backlog is in review.

REVIEW mode was checked first. Each of the six open bot-audit PRs already has a prior review by the bot's git identity (`TsRun`); all are recorded as `COMMENTED` state because GitHub blocks self-approval when the PR author and reviewer share an identity. Since no un-reviewed bot-audit PR exists, REVIEW had nothing to do.

DISCOVERY was skipped (`docs/audit/features.json` exists and is non-empty). TEST_ONE was blocked by the queue-full guardrail. No branch was created, no PR was opened, no `features.json` entry was bumped.
