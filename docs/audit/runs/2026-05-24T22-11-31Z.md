# Audit run — 2026-05-24T22:11:31Z
**Mode:** TEST_ONE
**Subject:** signin-flow
**Result:** OK

Picked `signin-flow` (oldest `lastTestedAt` among features without an open bot-audit PR — `landing-page-render`, `trainer-studies-list`, `settings-page`, `student-dashboard`, and `tour-page` are excluded because their open PRs #78/#75/#81/#86/#89 cover them).

Wrote `tests/audit/signin-flow.spec.ts` and ran it via `playwright.audit.config.ts` against prod (`https://praxis.tsrun.dev`). The spec exercised the primary action: filling email + password on the landing-page form and asserting the redirect to `/trainer/studies`. That action passed cleanly — `PAGE ERRORS: []`, `APP CONSOLE ERRORS: []`, redirect happened within 15s. Email focus shows `outlineStyle: 'solid', outlineWidth: '2px', outlineColor: 'rgb(251, 191, 36)'` (global `:focus-visible` working).

Three UI issues were observed on the sign-in form, but ALL THREE are already addressed by open PR #78 (`claude/improve-landing-page-render-2026052412`, labelled `bot-audit`, already reviewed and awaiting human merge), which edits the same source file `src/auth/SignInUpForm.tsx`:

1. **Email + Password inputs have no label association** (`labelsFor: 0`, no `htmlFor`, no `aria-label`, no wrapping `<label>`). PR #78 wires `useId()` → `id` + `htmlFor` on both inputs and adds `type="email"` to the email input.
2. **Mode toggle buttons "Sign in" / "Create account" expose no pressed state** (`ariaPressed: null`, `role: null`). PR #78 adds `aria-pressed={mode === 'in'}` / `aria-pressed={mode === 'up'}` on those two buttons, plus `role="group" aria-label="Account mode"` on the container.
3. **Mobile (375×812) page has 7px of horizontal overflow** (`scroll: 382, client: 375`). Traced to the absolutely-positioned radial-gradient glow in `src/auth/LandingPage.tsx` L140-150 (`inset: '-10% -10% auto -10%'` extends 10% beyond the hero column). PR #78 fixes this at the page level by adding `overflow-x: clip` to `body` in `src/index.css`.

Creating a parallel "improve signin-flow" PR would conflict line-for-line with PR #78 in the same file. The signing-in action itself works on the current prod build — no application errors, the redirect succeeds. Marking signin-flow `lastTestedAt` bumped, `lastResult="ok"` so the rotation moves on. After PR #78 merges, the next signin-flow rotation will re-test against the updated form a11y.

Transport: Playwright via `playwright.audit.config.ts`, single chromium worker, prod `https://praxis.tsrun.dev`.
