# Audit run: 2026-05-23T18:09:32Z

**Mode:** TEST_ONE  
**Feature:** `student-dashboard` (Student dashboard, route `/student/dashboard`, requiresAuth: true)  
**Result:** OK

The Playwright spec logged in via the landing-page form (`input[placeholder="you@studio.club"]` + `input[type="password"]` + `button[type="submit"]`), navigated to `https://praxis.tsrun.dev/student/dashboard`, and confirmed HTTP 200. The page rendered all expected sections: a personalised greeting ("Good evening, Claude Bot"), ACTIVE (0) and COMPLETED (0) stat tiles, "No active assignments" / "All assignments 0" panels with Active/Completed filter tabs, a "Your trainer" card (0 studies assigned), a "Today" card (0% average, 0 done, 0 active), and an "Activity" card ("No activity yet."). A keyboard tip card was also visible. Screenshot captured to `tests/audit/screenshots/student-dashboard.png`.

The sole console error was the browser-generated `"Failed to load resource: the server responded with a status of 401 ()"` from `GET /api/auth/me` on the initial anonymous page load (before login). This is correct HTTP semantics and was already noted in prior runs as non-actionable — the browser itself generates this message for any non-2xx fetch and it cannot be suppressed from JavaScript. No uncaught page errors. `features.json` bumped (`lastTestedAt`, `lastResult: ok`) and pushed to main via GitHub MCP (git push returns 403 in this sandbox).
