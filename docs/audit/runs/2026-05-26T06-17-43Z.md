# Audit run — 2026-05-26T06:17:43Z
**Mode:** TEST_ONE
**Subject:** trainer-students-filters
**Result:** IMPROVED+PENDING

Exercised the All / Linked / Invited segmented filter on `/trainer/students`. The
control itself is sound: `role="group"` + `aria-label="Filter students by status"`
on the container, `aria-pressed` toggles correctly on each button, the `.active`
class swaps, `Tab` from "All" lands on "Linked", `Space` activates the focused
tab, and tap on the mobile viewport switches state. Page errors `[]`, app console
errors `[]`.

Layout bug confirmed at viewport **375×812**: the filter row has
`display: flex; align-items: center; gap: 12; flex-wrap: nowrap`. With only
~308 px of usable width inside the card, the search input (`flex: 1, maxWidth: 360`,
no min) collapses to roughly **44 px** — just the magnifier icon, the
`Find by nickname…` placeholder is invisible. The `0 students` count meta
then wraps onto **two lines** because there isn't enough room left after the
179 px-wide segmented control. The audit spec captured
`MOBILE FILTER ROW LAYOUT: { rowWidth: 336, rowFlexWrap: 'nowrap', segWidth: 179,
segRight: 264, countRight: 342, windowWidth: 375 }` — the count visually
extends past the card's right edge.

Fix in `src/trainer/StudentsPage.tsx`:
- Filter row: `flexWrap: 'wrap'` + `rowGap: 10` so children can drop to a
  second row when there isn't enough horizontal space.
- Search wrapper: `flex: '1 1 220px'` (was `flex: 1, maxWidth: 360`) so the
  search input keeps a 220 px minimum and wraps to its own row at narrow
  widths instead of being squashed to an icon-only nub.
- Count `.meta`: `whiteSpace: 'nowrap'` so `0 students` stays on one line.

On desktop (≥ ~480 px usable inside the card) the row still fits in a single
line — layout is unchanged from prod. Verified with desktop 1280×900 +
mobile 375×812 screenshots.

Verification:
- `npm run typecheck` ✓
- `npm test` ✓ (37/37)
- `npx playwright test --config=playwright.audit.config.ts tests/audit/trainer-students-filters.spec.ts` ✓ against prod
