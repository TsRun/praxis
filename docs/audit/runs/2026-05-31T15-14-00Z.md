# Audit run — 2026-05-31T15:14:00Z
**Mode:** TEST_ONE
**Subject:** student-opening-study-viewer
**Result:** IMPROVED+PENDING+verification

Exercised the Student opening study viewer against prod with the existing audit
spec. The viewer reaches the error path because the backend `/api/student/studies/opening/3`
endpoint currently returns HTTP 500 with the raw Postgres message
`column "root_pgn" does not exist`. The error card renders correctly with the
friendly heading and back-to-dashboard link — but it also surfaced the raw
SQL error string verbatim under the user-facing copy. That's a UI quality leak:
a student is shown internal database schema details that are not actionable
and look alarming. The backing 500 itself is a server/database concern and
out of scope (`server/db/`, `server/migrations/` forbidden).

Smallest-change fix in `src/student/OpeningStudyViewer.tsx`: added a small
`userVisibleErrorDetail` helper that suppresses well-known internal error
patterns (e.g. `column "X" does not exist`, `syntax error`, `null value in
column`, `permission denied for`, etc.) before rendering them in the error
card. HTTP-shaped fallbacks like `GET /api/... → 500` still pass through. The
friendly explanatory paragraph above is unchanged, so the user still sees
context. Verified locally: typecheck ✅, vitest 37/37 ✅, audit spec on prod ✅
(mobile pass at 375x812 still clean; focus outline yellow 2px solid OK; no
uncaught page errors; only the documented 401/500 resource-load console
warnings which are waived).
