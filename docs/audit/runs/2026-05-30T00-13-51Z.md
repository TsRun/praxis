# Audit run — 2026-05-30T00:13:51Z
**Mode:** TEST_ONE
**Subject:** trainer-studies-list
**Result:** IMPROVED+PENDING+verification

Ran the trainer-studies-list audit spec against prod. Spec passed: heading, stat
tiles, all three section headings render; search input has `aria-label="Search
studies"`; segmented filter group has `aria-label`; view-toggle icon buttons
have `aria-label` + `aria-pressed`; `#new-study-trigger` exposes
`aria-haspopup`/`aria-expanded`; focus moves to the All filter with a 2px
amber outline; mobile 375x812 fits with no horizontal overflow (scrollWidth
360 ≤ clientWidth 375); zero page errors and zero app console errors.

UI finding: the small count badge next to each section heading
("Opening studies 0", "Game studies 0", "Tactical sets 0") rendered in
`var(--text-faint)` = `#6b6b73` at 13px on the page's dark surface. That is
roughly a 3.77:1 contrast ratio, below the WCAG-AA 4.5:1 floor for normal
text. Smallest-change fix: bump the count `<span>` to `var(--text-dim)`
(`#a0a0a8`, ~9:1) and add an `aria-label` so screen readers announce
"3 items" rather than just "3". Single-line CSS-token change in
`src/trainer/StudiesPage.tsx`, no markup or layout change.
