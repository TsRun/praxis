# Audit run — 2026-06-01T03:13:20Z
**Mode:** TEST_ONE
**Subject:** trainer-import-lichess
**Result:** IMPROVED+PENDING+PR

Picked oldest non-tested feature `trainer-import-lichess` (lastTestedAt 2026-05-29T00:15:00Z). Ran `tests/audit/trainer-import-lichess.spec.ts` against prod — spec passes: dialog renders, role/aria-modal/aria-label correct, Esc closes, focus-visible outline on side picker, no console/page errors, mobile 375×812 has no overflow.

UI quality issue observed: the dialog header's Close button measures 34×28 px (logged as `CLOSE BUTTON BOX: { width: 34, height: 28 }`). That's below WCAG 2.5.5 AAA (44×44) and below iOS/Android touch target guidance — easy to miss-tap on mobile where this dialog is shown full-bleed. The button uses the shared `Dialog` component (`src/components/ui/Dialog.tsx`) with `Btn variant="ghost" size="sm"` (btn-sm height: 28px, padding: 0 10px). A second close button in `src/trainer/InviteStudentDialog.tsx` has the same dimensions. Fix is to enforce a 36×36 minimum hit area via inline `style={{ minWidth: 36, minHeight: 36, padding: 0, justifyContent: 'center' }}` on both Btn instances — keeps the small visual icon (14×14) and ghost styling, but doubles the clickable area on the height axis. Two-file, eight-LOC change. Typecheck + 37 unit tests still pass.
