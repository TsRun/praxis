# Audit run — 2026-05-23T20:13:12Z

**Mode:** TEST_ONE · **Feature:** `trainer-games-browser` (`/trainer/games`, auth)

Selection: `tour-page` is alphabetically first among null-`lastTestedAt` features, but PR #60 is already open against it from the prior run, so this run picked the next id in order — `trainer-games-browser`.

Playwright signed in via the standard `/` form (Sign in → button) using `PRAXIS_BOT_EMAIL` / `PRAXIS_BOT_PASSWORD`, persisted storage state to `.auth/bot.json`, then navigated to `/trainer/games`. The page returned 200 with no error overlay and rendered as expected:

- Heading `Browse games` visible.
- Source tabs `Chess.com` / `Lichess` / `Database` all present and clickable; switching tabs swapped the username/player input correctly (Lichess placeholder `Hikaru`, Database placeholder `Carlsen`).
- Filters card (Color / Year / Result / ECO / Min Elo / Time control) rendered with sensible defaults.
- The "Find games passing through this position" panel rendered the `PlayFromStartBoard` with both `Undo` and `Reset to start` buttons.
- Right pane showed the empty state "Run a search to see games." — correct.

No uncaught page errors. Console produced exactly one entry — Chrome's "Failed to load resource: 401" for `/api/auth/me` during the anonymous bootstrap before sign-in completed — which is the expected behaviour the browser logs for any non-2xx fetch, not an application-emitted error. Same noise was waived in prior runs.

Result: **OK**. Bumped `lastTestedAt` on `trainer-games-browser` in `docs/audit/features.json` and committed to main. Screenshot saved to `tests/audit/screenshots/trainer-games-browser.png` (gitignored). The transient Playwright spec at `tests/audit/trainer-games-browser.spec.ts` stayed gitignored per the OK-path convention.

Git transport note: local `git push` returned HTTP 403 again (same as the 19:20 run); files were committed via the GitHub MCP `push_files` tool instead.
