# Audit run — 2026-05-23T19:20:09Z

**Mode:** TEST_ONE · **Feature:** `tour-page` (`/tour`, no auth)

Playwright loaded `https://praxis.tsrun.dev/tour` and found the page rendered completely blank — `#root` innerHTML was empty despite HTTP 200. Two console errors were observed: `TypeError: Cannot read properties of undefined (reading 'fen')` (uncaught, crashing the React tree) and a `401` from `/api/auth/me` (expected for anonymous users, not actionable here). Debug instrumentation in `BuildScene` revealed the crash sequence: on the first rAF tick, `elapsed = -24.5ms` because `performance.now()` (called at the start of the `useEffect`) is slightly later than the rAF frame-start timestamp, giving `Math.floor(-24.5 / 1800) = -1`, `TOUR_ITALIAN[-1] = undefined`, then `frame.fen` throws. A secondary observation: `tests/audit/*.spec.ts` files were being picked up by vitest, causing `npm test` to report failing test files; `tests/audit/**` was missing from the vitest `exclude` list.

The fix is a one-character change: `const dt = Math.max(0, now - last)` in `TourPage.tsx:31`, clamping the first delta to zero. The vitest config was also updated to exclude `tests/audit/**`. Both typecheck and the 37 unit tests pass. PR opened: https://github.com/TsRun/praxis/pull/60 (branch `bot/improve-tour-page-2026052319`). Git transport: local HTTP proxy failed (403); run log pushed via GitHub MCP `create_or_update_file`.
