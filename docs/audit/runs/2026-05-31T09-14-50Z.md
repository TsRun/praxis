# Audit run — 2026-05-31T09:14:50Z
**Mode:** DISCOVERY
**Subject:** n/a
**Result:** DISCOVERED (+1 -0 kept 20)

Cycle complete: all 20 non-removed features had `lastTestedAt > lastDiscoveryAt` (last discovery 2026-05-28T06:23:55Z), so DISCOVERY mode triggered. Crawled the public landing, `/tour`, and (after logging in as the bot account) `/trainer/studies`, `/trainer/students`, `/trainer/games`, `/student/dashboard`, `/settings`, plus bare `/trainer` and `/student` routes and the unauthenticated `/signup` `/login` `/pricing` paths.

All 20 existing features confirmed present on prod with the same headings, tabs and primary buttons captured in their descriptions. Bare `/trainer` and `/student` 302 to `studies` / `dashboard`; `/signup` `/login` `/pricing` all redirect back to `/` (no dedicated routes). One new surface found that no feature covered: clicking the **Free editor** source tab on `/trainer/games` swaps the right panel from the games list to a position editor with a piece picker (king/queen/rook/bishop/knight/pawn × both colours), a delete-piece (⌫) control, and **Starting position** / **Empty board** reset buttons. Added as `trainer-games-free-editor` with `lastTestedAt=null` so it sorts to the head of the next TEST_ONE rotation. Bumped `lastDiscoveryAt` to now and `discoveryCount` 2→3.
