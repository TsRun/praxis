# Audit run — 2026-06-01T15:13:00Z
**Mode:** TEST_ONE
**Subject:** profile-menu
**Result:** OK+MERGED

Exercised the profile-menu dropdown at /trainer/studies on prod (claude.bot signed in).
Trigger button: aria-haspopup="menu", aria-label="Open profile menu",
aria-controls="user-menu", aria-expanded toggles correctly on open/close.
Focus ring is visible (2px solid amber outline, offset 2px) and ESC closes
the menu (aria-expanded → false, no [role=menu] in DOM). Outside-click also
closes the menu. Menu content shows name, email, role chips (Trainer / Student
/ Own trainer), "Open settings →" link, "Quick roles" and "Sign out" buttons.
Quick-roles checkboxes are each wrapped in a `<label>` carrying the role
text, so they have an implicit accessible name.

At 375x812 the trigger sits at x=181 right=320 (width 139, "CB Claude Bot");
the open menu fits at left=20 right=320 (width 300, viewport 375) — no
horizontal overflow (scrollWidth 360 < clientWidth 375), no overflow off
the left or right edges. Desktop screenshot at 1280 shows the popup
correctly overlaying the KPI cards.

No application console errors, no uncaught page errors. No regressions
observed; bumping lastTestedAt only.
